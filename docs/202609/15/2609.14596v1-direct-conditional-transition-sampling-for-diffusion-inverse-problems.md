---
title: Direct Conditional Transition Sampling for Diffusion Inverse Problems
title_zh: 用于扩散逆问题的直接条件转移采样
authors: "Qi Yu, Hanlin Wu, Xiaohui Sun"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14596v1"
tags: ["query:tfree-diff"]
score: 8.0
evidence: 免训练扩散逆问题求解与直接随机流采样优化
tldr: 扩散逆问题中，免训练求解器常需在局部测量引导与昂贵的干净空间后验更新间权衡，独立后验刷新虽能全局校正，却依赖ODE积分与MCMC。本文提出直接条件转移采样DCTS，用直接随机流近似理想刷新目标，沿短内路径估计测量条件干净均值，并将高斯源噪声直接输运到下一噪声状态。借助去噪器兼容充分统计量与协方差缩放算子更新实现该估计。在四类逆问题上，DCTS重建质量有竞争力，相比竞争方法最高提速16.8倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14596v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 734, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14596v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 529, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.14596v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 715, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.14596v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 231, \"label\": \"Table\"}]"
motivation: 免训练扩散逆求解器难以兼顾局部测量引导的廉价性与全局后验校正，独立后验刷新又依赖昂贵ODE和MCMC。
method: DCTS直接近似条件转移：沿短内路径估计测量条件干净均值，并用去噪器兼容充分统计量与协方差缩放算子更新，将源噪声输运到下一噪声态。
result: 在四类逆问题上，DCTS重建质量具竞争力，相较竞争方法最高实现16.8倍加速，且无需额外训练。
conclusion: 该方法为免训练扩散逆问题提供高效直接条件转移采样路径，避免显式干净采样与昂贵MCMC，兼顾全局校正与速度。
---

## 摘要
无需训练的扩散逆问题求解器通常需要在局部测量引导与代价高昂的干净空间后验更新之间进行选择。独立后验刷新可通过采样一个干净条件样本并重新加噪来改善全局校正，但其实际实现需要概率流 ODE 积分和干净空间马尔可夫链蒙特卡洛（MCMC）。我们提出直接条件转移采样（Direct Conditional Transition Sampling, DCTS），这是对同一理想刷新目标的一种直接随机流近似。DCTS 不显式抽取干净样本，而是沿一条较短的内部路径估计以测量为条件的干净均值，并将高斯源噪声直接传输到下一个噪声状态。与去噪器兼容的充分统计量以及协方差缩放的算子更新使这种条件均值估计成为可能。在四个逆问题上的实验表明，DCTS 在重建质量上具有竞争力，并且相比竞争方法实现了最高 16.8 倍的加速。

## Abstract
Training-free diffusion inverse solvers typically choose between local measurement guidance and costly clean-space posterior updates. Independent posterior refresh can improve global correction by sampling a clean conditional and re-noising it, but its practical realization requires probability-flow ODE integration and clean-space Markov chain Monte Carlo (MCMC). We propose Direct Conditional Transition Sampling (DCTS), a direct stochastic-flow approximation to the same ideal refresh target. Rather than explicitly drawing a clean sample, DCTS estimates the measurement-conditioned clean mean along a short inner path and transports Gaussian source noise directly to the next noisy state. A denoiser-compatible sufficient statistic and a covariance-scaled operator update enable this conditional-mean estimation. Experiments on four inverse problems demonstrate that DCTS achieves competitive reconstruction quality with up to $16.8\times$ speedups over competing methods.

---

## 论文详细总结（自动生成）

# 论文总结：Direct Conditional Transition Sampling for Diffusion Inverse Problems

## 1. 核心问题与整体含义

- **研究背景**：扩散逆问题求解的目标是从观测 \(y=A(z)+n\) 中恢复未知信号 \(z\)，其中 \(A\) 为已知前向算子，\(n\sim\mathcal N(0,\Sigma_y)\)。该问题通常病态，存在多个合理解，因此采用贝叶斯后验 \(p(z|y)\propto p(y|z)p_{\text{data}}(z)\) 刻画不确定性。
- **免训练扩散求解器现状**：利用预训练扩散模型作为先验，无需针对每个算子重新训练条件模型。现有方法主要分两类：
  - **局部测量引导**：如 DPS、DDRM、PiGDM、CoDPS、TMPD 等，在反向扩散的每一步用测量残差修正。其问题是连续噪声状态间更新较小，难以纠正早期步骤产生的全局误差，尤其在非线性逆问题中。
  - **干净空间更新**：如 DDNM、DiffPIR、DAPS。DAPS 通过“干净空间后验刷新”实现全局校正：先用概率流 ODE 从 \(x_t\) 得到干净估计，再用 MCMC 近似采样 \(Z\sim p(z|x_t,y)\)，最后重新加噪得到 \(x_{t'}\)。但该过程需要 ODE 积分和 MCMC，计算代价高。
- **核心问题**：能否不显式进行干净空间采样和重新加噪，而是直接从当前噪声状态 \(x_t\) 转移到下一噪声状态 \(x_{t'}\)，同时实现与理想后验刷新相同的目标？
- **整体含义**：论文提出 **Direct Conditional Transition Sampling (DCTS)**，将独立后验刷新重新表述为直接随机流输运。它保持 DAPS 式全局校正能力，但避免显式干净采样、MCMC 和重新加噪，从而在重建质量与运行效率之间取得更好平衡。

## 2. 方法论

- **核心思想**：DCTS 不显式抽取干净样本 \(Z\)，而是构造一条从高斯源噪声到刷新终点的内部条件流，并沿该流估计测量条件干净均值，进而将源噪声直接输运到下一噪声状态。
- **理想刷新核**：
  - 定义噪声后验边缘 \(\pi_t(x|y)=\int q_t(x|z)p(z|y)\,dz\)。
  - 理想独立后验刷新核为  
    \[
    K^\star_{t\to t'}(x'|x,y)=\int q_{t'}(x'|z)p(z|x,y)\,dz
    \]
    若 \(X_t\sim\pi_t\)，采样 \(Z\sim p(\cdot|X_t,y)\) 并重新加噪，可得到 \(\pi_{t'}\)。DCTS 的目标是以直接流近似该核。
- **内部随机路径**：
  - 设内部坐标 \(s\in[0,1]\)，线性插值：
    \[
    R_s=(1-s)\sigma_{\text{src}}\eta+s(Z+\sigma_{t'}\eta),\quad \eta\sim\mathcal N(0,I)
    \]
    等价于 \(R_s=sZ+\bar\sigma_s\eta\)，其中 \(\bar\sigma_s=(1-s)\sigma_{\text{src}}+s\sigma_{t'}\)。
  - 起点 \(R_0=\sigma_{\text{src}}\eta\) 易采样，终点 \(R_1=Z+\sigma_{t'}\eta\) 符合刷新端点分布。
  - 根据 Flow Matching 的边际速度恒等式，构造速度场只需要条件干净均值  
    \[
    m_s^\star=\mathbb E[Z|R_s=r_s,X_t=x_t,y]
    \]
- **测量条件干净均值估计**：
  - **第一步：合并两个噪声观测**。由于 \(X_t\) 和 \(R_s\) 在给定 \(Z\) 下条件独立，可构造与预训练去噪器兼容的充分统计量：
    \[
    \tilde X_s=v_s\left(\frac{X_t}{\sigma_t^2}+\frac{sR_s}{\bar\sigma_s^2}\right)=Z+\tilde\sigma_s\tilde\epsilon_s
    \]
    其中 \(v_s=(\sigma_t^{-2}+s^2/\bar\sigma_s^2)^{-1}\)，\(\tilde\sigma_s=\sqrt{v_s}\)。当 \(s=0\) 时退化为 \(X_t\)；随 \(s\) 增大逐步融入内部状态信息。
  - 利用 Tweedie 公式，用预训练去噪器 \(D\) 得到

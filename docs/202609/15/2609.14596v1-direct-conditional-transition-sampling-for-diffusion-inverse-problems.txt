Title: Direct Conditional Transition Sampling for Diffusion Inverse Problems

URL Source: https://arxiv.org/pdf/2609.14596v1

Published Time: Tue, 15 Sep 2026 01:48:15 GMT

Number of Pages: 5

Markdown Content:
## DIRECT CONDITIONAL TRANSITION SAMPLING FOR DIFFUSION INVERSE PROBLEMS 

## Qi Yu, Hanlin Wu, Xiaohui Sun 

## School of Information Science and Technology, Beijing Foreign Studies University 

ABSTRACT 

Training-free diffusion inverse solvers typically choose be-tween local measurement guidance and costly clean-space posterior updates. Independent posterior refresh can im-prove global correction by sampling a clean conditional and re-noising it, but its practical realization requires probability-flow ODE integration and clean-space Markov chain Monte Carlo (MCMC). We propose Direct Conditional Transition Sampling (DCTS), a direct stochastic-flow approximation to the same ideal refresh target. Rather than explicitly drawing a clean sample, DCTS estimates the measurement-conditioned clean mean along a short inner path and transports Gaussian source noise directly to the next noisy state. A denoiser-compatible sufficient statistic and a covariance-scaled op-erator update enable this conditional-mean estimation. Ex-periments on four inverse problems demonstrate that DCTS achieves competitive reconstruction quality with up to 16 .8×

speedups over competing methods. 

Index Terms — diffusion models, inverse problems, pos-terior sampling, flow matching, image restoration 

1. INTRODUCTION 

The goal of inverse problems is to recover an unknown signal 

z ∈ Rd from an observation y = A(z) + n, where A :

Rd → Rm is a known forward operator and n ∼ N (0, Σy ) is additive Gaussian noise. The problem is inherently ill-posed, as the potential nonlinearity and information loss of A can lead to multiple plausible solutions for a single observation. A Bayesian formulation characterizes this uncertainty using the posterior distribution 

p(z | y) ∝ p(y | z)pdata (z), (1) where pdata is the prior distribution and p(y | z) is the likeli-hood induced by the measurement model. Diffusion models [1, 2] provide an expressive learned prior and can therefore solve various inverse problems with one pretrained denoiser, without retraining a conditional model for each operator. The forward diffusion process generates a noisy state Xt from a clean signal Z through Gaussian perturbation: 

Xt = αtZ + σtϵt, ϵt ∼ N (0, I),𝑝(𝒛|𝒚)  

> Gaussian 𝑝(𝒛|𝒚)
> (a) Local guidance
> (b) DAPS: clean-space refresh
> (c) DCTS: direct conditional transition

𝒙 𝑡  Guided velocity 

> Measurement
> correction
> Pretrained velocity

𝒙 𝑡 ′

𝒙 𝑡 

𝒙 𝑡 ′    

> Previous trajectory
> Refreshed trajectory
> ②Clean-space MCMC
> Gaussian
> ①PF-ODE
> Measurement 𝒚
> ③Re-noise
> Gaussian

𝒙 𝑡  Previous trajectory 

> Refreshed trajectory
> 𝐕𝐞𝐥𝐨𝐜𝐢𝐭𝐲 = 𝒖 𝒔 𝒚 (𝒓 𝒔 |𝒙 𝒕 , 𝒚)
> 𝑝(𝒛|𝒚)

𝑟 0 𝑟 𝑠 

> Sample

𝑟 1 = 𝒙 𝑡 ′ 

> Measurement 𝒚

Fig. 1 . Comparison of three conditional transitions: (a) local guidance; (b) DAPS clean-space refresh; and (c) our proposed DCTS, which directly transports noise to the next state. where αt and σt are known schedule coefficients. Equiva-lently, the forward diffusion kernel is 

qt(x | z) := N (x; αtz, σ 2 

> t

I). (2) We adopt the variance-exploding (VE) parameterization [2], for which αt ≡ 1. Using score estimates from a pretrained denoiser, we can numerically solve the reverse-time stochas-tic differential equation (SDE) to generate samples from the learned data prior. For inverse problems, our goal is to use this denoiser to sample from the posterior p(z | y), which requires incorporating the measurement y into the sampling process. Existing training-free solvers employ two broad strate-gies for posterior sampling: local guidance and clean-space updates. Guidance methods modify each reverse diffusion step using a tractable approximation to the noisy likelihood score. DPS [3] differentiates a measurement residual evalu-ated at a denoised estimate, whereas DDRM [4], PiGDM [5], and CoDPS [6] exploit linear-operator structure or a Gaussian approximation. TMPD [7] and optimal posterior covariance methods [8] further incorporate denoiser uncertainty into the measurement update. However, small updates between con-secutive noisy states can make it difficult to correct global arXiv:2609.14596v1 [cs.CV] 13 Sep 2026 errors from earlier sampling steps, particularly in nonlinear inverse problems [9]. A second class incorporates measurements through ex-plicit updates in clean space, then generates the next noisy state. DDNM [10] enforces data consistency in denoised es-timates, while DiffPIR [11] solves a clean-space data sub-problem between denoising steps. DAPS [9] refreshes the noisy state through clean-space sampling and independent re-noising, allowing transitions beyond local trajectory correc-tions. Each refresh involves three steps: (i) integrating a probability-flow ODE from xt to obtain a clean estimate; (ii) initializing MCMC from this estimate to approximately sam-ple Z from p(z | xt, y); and (iii) re-noising via Xt′ = Z +

σt′ ξ, where ξ ∼ N (0, I) is fresh Gaussian noise. The cost of repeated ODE integration and MCMC sampling motivates the following question: 

Can we realize the same posterior refresh through a di-rect transition from xt to xt′ , without explicit clean-space sampling and re-noising? 

We answer this question with Direct Conditional Transi-tion Sampling (DCTS), a training-free method that replaces explicit clean-space sampling and re-noising with a direct transition based on Flow Matching [12]. Along this flow, we combine the current noisy state xt and the evolving flow state into a single Gaussian-corrupted state compatible with the pretrained denoiser. The denoiser provides a clean-signal estimate, which we refine using the measurement y through a Gaussian update tailored to the forward operator. This re-fined estimate determines the flow velocity that transports fresh Gaussian noise directly to the next noisy state xt′ . As illustrated in Fig. 1, the resulting transition retains the same posterior-refresh target while avoiding explicit clean-space sampling, MCMC refinement, and re-noising. Our contribu-tions are threefold: 1) We formulate independent posterior refresh as a di-rect stochastic transport, enabling global corrections beyond the constraints of local trajectory updates. 2) We develop a training-free method for estimating trans-port velocities conditioned on measurements using a pretrained denoiser. 3) Experiments on diverse inverse problems demonstrate that DCTS achieves competitive reconstruction quality with substantially reduced computational cost. 

2. METHOD 2.1. Problem Formulation 

Conditioned on an observation y, the forward diffusion kernel in Eq. (2) defines the noisy posterior marginal 

πt(x | y) := 

Z

qt(x | z) p(z | y) d z. (3) We aim to realize the independent posterior-refresh kernel: 

K⋆t→t′ (x′ | x, y) = 

Z

qt′ (x′ | z)p(z | x, y) d z. (4) This kernel maps the noisy posterior marginal πt to πt′ : if 

Xt ∼ πt(· | y), sampling Z ∼ p(· | Xt, y) yields the clean posterior marginal p(z | y), and re-noising through qt′ yields 

πt′ (· | y). The posterior refresh kernel defined in Eq. (4) is the same as that of DAPS [9], but DCTS realizes it differ-ently. DAPS explicitly approximates the clean draw from 

p(· | xt, y) and then re-noises it. In contrast, we define an inner conditional flow for each outer step t → t′. Condi-tioned on xt and y, we sample xt′ by integrating the flow ODE from fresh Gaussian noise, bypassing explicit clean-space sampling and re-noising. We next present a training-free construction of the flow’s velocity field using a pretrained denoiser. 

2.2. Stochastic Inner Path 

We build on the GLASS [13] construction of Gaussian tran-sitions, adapting it to inverse problems by using the measure-ments and the forward operator to refine the denoiser’s clean-signal estimate. Let s ∈ [0 , 1] denote an inner coordinate, distinct from the outer diffusion time. We linearly interpolate between a Gaussian source and the refresh endpoint: 

Rs = (1 − s)σsrc η + s(Z + σt′ η), η ∼ N (0, I). (5) Here σsrc > 0 is an auxiliary source scale and η is indepen-dent of (Z, X t, y). Equivalently, Rs = sZ + ¯ σsη, where 

¯σs = (1 − s)σsrc + sσ t′ . The source R0 = σsrc η is easy to sample, while R1 = Z + σt′ η has the endpoint law in Eq. (4). By the marginal velocity identity of Flow Match-ing [12], the marginal velocity at rs is the conditional expec-tation of the sample-wise path velocity given the current state and conditioning variables. For the linear path in Eq. (5), this expectation depends on the clean endpoint only through the conditional clean mean 

m⋆s := E[Z | Rs = rs, X t = xt, y]. (6) Therefore, estimating m⋆s along the inner path is sufficient to construct the velocity field. 

2.3. Measurement-Conditioned Clean Mean 

We estimate m⋆s in two stages. First, before incorporating y,we estimate the prior clean mean E[Z | Xt = xt, R s = rs].This mean depends on two noisy states, whereas a pretrained denoiser accepts only a single Gaussian-corrupted state and its noise level. Since Xt and Rs are conditionally independent given Z, we follow the derivation in [13] to combine these two Gaussian observations into a single sufficient statistic for 

Z that preserves this conditional mean: 

eXs := vs

 Xt

σ2

> t

+ sR s

¯σ2

> s



= Z + eσs eϵs, (7) where vs = ( σ−2 

> t

+ s2/¯σ2 

> s

)−1, eσs = √vs, and eϵs ∼ N (0, I).The first equality defines eXs as a precision-weighted aver-age of the two states, and the second shows that it remains a Gaussian-corrupted version of Z. At s = 0 , R0 contains no information about Z, so eX0 = Xt and eσ0 = σt; as s in-creases, eXs progressively incorporates information from the evolving inner state. For the observed states (xt, rs), its real-ization exs = vs(xt/σ 2 

> t

+srs/¯σ2 

> s

) and noise level eσs therefore form a valid denoiser input. Moreover, the sufficiency of eXs

for Z ensures that E[Z | Xt = xt, R s = rs] = E[Z | eXs =

exs]. By Tweedie’s formula [14], one evaluation of the pre-trained denoiser D on this Gaussian-corrupted statistic esti-mates the same clean conditional mean: 

ms = D(exs, eσs) ≈ E[Z | Xt = xt, R s = rs]. (8) In the second stage, we incorporate y using a local Gaus-sian approximation [5] with covariance scale κ > 0:

p(z | xt, rs) ≈ N (z; ms, c sI), cs = κv s, (9) 

Linear operators. Following Gaussian conditioning ap-proaches [5, 6], for A(z) = Az , Gaussian conditioning cor-rects ms to 

my 

> s

= ms + csA⊤(csAA ⊤ + Σy )−1(y − Am s). (10) The inverse balances this local covariance against measure-ment noise, making Eq. (10) the exact posterior mean un-der approximation (9). Eq. (10) is evaluated using operator-specific closed-form or iterative solvers without forming the inverse explicitly, with implementation details in Sec. 3.1. 

Nonlinear operators. For a differentiable nonlinear op-erator, let Js = ∇A (ms) denote its Jacobian at the current mean. Substituting its local linearization into the Gaussian update gives the Gauss–Newton moment 

my 

> s

= ms + csJ⊤ 

> s

(csJsJ⊤ 

> s

+ Σy )−1[y − A (ms)] . (11) We evaluate Eq. (11) using Jacobian-vector products without explicitly forming Js; implementation details are provided in Sec. 3.1. Unlike the linear update, the nonlinear update is only locally valid and can be inaccurate for strongly multimodal conditionals. 

2.4. Direct Transition Velocity 

Having estimated the measurement-conditioned clean mo-ment, we now convert it into the velocity that transports the Gaussian source to the refresh endpoint. Differentiating the inner path in Eq. (5) and substituting η = ( Rs −sZ )/¯σs gives the sample-wise velocity ˙Rs = bsRs + (1 − sb s)Z, where 

bs = ( σt′ − σsrc )/¯σs for s ∈ [0 , 1) . Taking its conditional ex-pectation and replacing the unknown clean conditional mean by my 

> s

gives the practical velocity field 

uy 

> s

= bsrs + (1 − sb s)my 

> s

. (12) 

Algorithm 1 Direct Conditional Transition Sampling 

Require: Denoiser D, measurement (y, A, Σy ), schedule 

σ0 > · · · > σ L = 0 , inner steps M , σsrc , and κ 

> 1:

Sample x(0) ∼ N (0, σ 20 I) 

> 2:

for k = 0 , . . . , L − 1 do  

> 3:

Set (xt, σ t, σ t′ ) = ( x(k), σ k, σ k+1 ) 

> 4:

Sample r0 = σsrc η, η ∼ N (0, I) 

> 5:

for j = 0 , . . . , M − 1 do  

> 6:

Set s = j/M and form (exs, eσs) by Eq. (7)  

> 7:

ms ← D(exs, eσs) 

> 8:

Compute my 

> s

by Eq. (10) or (11)  

> 9:

Update rs by Eqs. (12)–(13)  

> 10:

end for  

> 11:

x(k+1) ← r1 

> 12:

end for  

> 13:

return x(L)

The first term changes the innovation scale from σsrc to σt′ ,while the second transports the measurement-conditioned clean content. If my 

> s

= m⋆s , this is the exact marginal ve-locity for the inner path, and exact integration recovers the endpoint law in Eq. (4) without explicitly drawing Z. In practice, DCTS recomputes the approximate moment my 

> s

at each numerical inner state. For each outer transition t → t′, we first draw a fresh source r0 = σsrc η and set ∆s = 1 /M . At each s = j/M , we evaluate the conditional moment and velocity at the current 

rs, then apply the Euler update 

rs+∆ s = rs + ∆ s uy 

> s

. (13) After M steps, we set xt′ = r1. Each inner step requires one denoiser forward and one measurement-conditioned mo-ment update, so L outer transitions use LM of each, with no denoiser backward pass or variable-length clean-space chain. Algorithm 1 summarizes the complete sampler. 

3. EXPERIMENTS 3.1. Experimental Setup Dataset and metrics. Following DAPS [9], we use the first 100 test images from the FFHQ-256 dataset [15] to evaluate the effectiveness of DCTS. We report PSNR, SSIM, LPIPS, and ArcFace identity similarity [16]. LPIPS measures the per-ceptual difference between the reconstruction and the refer-ence image (lower is better), while ArcFace assesses facial identity preservation (higher is better). 

Inverse problems. We consider four inverse problems: super-resolution (SR), phase retrieval, motion deblurring, and grayscale colorization, all with Gaussian measurement noise 

σy = 0 .05 . For SR, we adopt the antialiased 4× downsam-pling operator from DAPS [9]. Phase retrieval is a nonlinear inverse problem of reconstructing an image from its Fourier Table 1 . Quantitative results on FFHQ-256. Best and second-best results are shown in bold and underline, respectively.                                                                                                                  

> Task Method PSNR (dB) ↑SSIM ↑LPIPS ↓ArcFace ↑Time (s) ↓
> SR ×4DAPS [9] 29.360 0.783 0.192 0.926 94.74 DPS [3] 24.663 0.705 0.259 0.619 53.02 MGPS [18] 28.189 0.791 0.163 0.879 81.20 MGDM [19] 28.526 0.807 0.151 0.900 82.10 DCTS 30.552 0.861 0.160 0.928 5.65
> Phase retrieval DAPS [9] 29.705 0.771 0.182 0.918 117.30 DPS [3] 19.065 0.550 0.394 0.439 53.78 MGPS [18] 28.439 0.806 0.165 0.874 81.61 MGDM [19] 28.665 0.801 0.170 0.859 83.55 DCTS (M=8) 29.696 0.812 0.170 0.908 25.17
> Deblurring DAPS [9] 31.651 0.836 0.137 0.971 66.26 DPS [3] 27.285 0.773 0.183 0.842 53.02 MGPS [18] 30.577 0.854 0.115 0.953 81.38 MGDM [19] 31.048 0.860 0.103 0.962 81.48 DCTS 32.678 0.886 0.128 0.969 7.41
> Colorization DAPS [9] 24.055 0.763 0.310 0.974 59.80 DPS [3] 11.700 0.526 0.594 0.761 53.37 MGPS [18] 23.001 0.877 0.217 0.964 80.22 MGDM [19] 22.051 0.865 0.226 0.962 81.36 DCTS 24.412 0.892 0.235 0.967 5.43

magnitudes without phase information; we use an oversam-pling rate of 2.0. For motion deblurring, we convolve images with a 61 × 61 motion blur kernel using reflection padding. Colorization uses a rank-one linear mapping from each RGB pixel to a scalar grayscale measurement. 

Implementation details. We adopt the pretrained DDPM from DAPS [9] as our denoiser. We set the source scale to 

σsrc = 1 and use L = 150 outer steps and M = 2 inner steps. For phase retrieval, we increase M to 8 to improve reconstruction quality. The outer noise levels use the EDM discretization [17] with polynomial exponent 7, σmax = 100 ,

σmin = 0 .1, and a final noise level of zero. The local covari-ance is csI with cs = κv s, where κ is 1.0 for SR, 0.5 for phase retrieval and motion blur, and 0.25 for colorization. To evaluate Eq. (10), we exploit the separable structure of the downsampling operator for SR and use pixelwise closed-form updates for colorization. For motion deblurring, we ap-proximately solve the corresponding linear system using four conjugate-gradient iterations. For phase retrieval, we evaluate Eq. (11) using analytic Jacobian products and four conjugate-gradient iterations. 

3.2. Main Results 

We compare with DAPS [9], DPS [3], MGPS [18], and MGDM [19], using their official default sampling hyperpa-rameters. Runtime is measured on an RTX 4090 GPU with a batch size of 1. Table 1 shows that DCTS attains the highest PSNR on three tasks, top-two mean ArcFace similarity on all four, and the lowest per-trajectory runtime. These results demonstrate that DCTS achieves competitive reconstruction quality while reducing runtime by avoiding repeated clean-space sampling. Fig. 2 shows that DCTS better preserves facial structure and details while reducing artifacts. 

Fig. 2 . Visual comparison for (a) SR × 4; (b) phase retrieval; (c) deblurring; (d) colorization. 

Table 2 . Ablation study on the phase retrieval task.                            

> Models PSNR(dB) ↑SSIM ↑LPIPS ↓ArcFace ↑
> Deterministic DDIM 13.618 0.238 0.644 0.115 Stochastic DDIM ( η= 1 )16.495 0.394 0.545 0.276 Point refresh ( M= 1 )28.813 0.761 0.214 0.881 DCTS ( M= 8 )29.696 0.812 0.170 0.908

3.3. Ablation Study 

We conduct an ablation study to evaluate the proposed tran-sition design and the effect of multiple inner updates. All variants use L = 150 outer steps, the same pretrained de-noiser, and the same measurement-conditioning rule. The DDIM [20] variants replace each DCTS transition with eight DDIM substeps starting from the current noisy state. Here, η

controls the stochasticity of DDIM: η = 0 yields determinis-tic updates, while η = 1 injects fresh Gaussian noise at each substep. We also evaluate point refresh, which generates the next noisy state by adding fresh Gaussian noise to a single measurement-conditioned clean estimate at each outer step. This corresponds to a single Euler step ( M = 1 ), whereas full DCTS recomputes the estimate along eight inner steps (M = 8 ). Point refresh therefore uses one eighth as many de-noiser calls as full DCTS. Table 2 reports the phase-retrieval results on the first 100 FFHQ-256 test images. Full DCTS achieves the best mean values on all four metrics. 

4. CONCLUSION 

We proposed DCTS, which reformulates independent pos-terior refresh as direct conditional transport without explicit clean-space sampling and re-noising. The flow recovers the ideal refresh kernel under exact conditional-mean estimation and integration. A denoiser-compatible sufficient statistic and local Gaussian conditioning enable a training-free approxi-mation. Experiments demonstrate competitive reconstruction quality with substantially reduced runtime. 5. REFERENCES 

[1] Jonathan Ho, Ajay Jain, and Pieter Abbeel, “Denoising diffusion probabilistic models,” in Advances in Neu-ral Information Processing Systems , 2020, vol. 33, pp. 6840–6851. [2] Yang Song, Jascha Sohl-Dickstein, Diederik P. Kingma, Abhishek Kumar, Stefano Ermon, and Ben Poole, “Score-based generative modeling through stochastic differential equations,” in International Conference on Learning Representations , 2021. [3] Hyungjin Chung, Jeongsol Kim, Michael T. McCann, Marc L. Klasky, and Jong Chul Ye, “Diffusion poste-rior sampling for general noisy inverse problems,” in 

International Conference on Learning Representations ,2023. [4] Bahjat Kawar, Michael Elad, Stefano Ermon, and Ji-aming Song, “Denoising diffusion restoration models,” in Advances in Neural Information Processing Systems ,2022, vol. 35, pp. 23593–23606. [5] Jiaming Song, Arash Vahdat, Morteza Mardani, and Jan Kautz, “Pseudoinverse-guided diffusion models for in-verse problems,” in International Conference on Learn-ing Representations , 2023. [6] Nebiyou Yismaw, Ulugbek S. Kamilov, and M. Salman Asif, “Gaussian is all you need: A unified framework for solving inverse problems via diffusion posterior sam-pling,” IEEE Transactions on Computational Imaging ,vol. 11, pp. 1020–1030, 2025. [7] Benjamin Boys, Mark Girolami, Jakiw Pidstrigach, Se-bastian Reich, Alan Mosca, and Omer Deniz Akyildiz, “Tweedie moment projected diffusions for inverse prob-lems,” Transactions on Machine Learning Research ,2024. [8] Xinyu Peng, Ziyang Zheng, Wenrui Dai, Nuoqian Xiao, Chenglin Li, Junni Zou, and Hongkai Xiong, “Improv-ing diffusion models for inverse problems using optimal posterior covariance,” in Proceedings of the 41st Inter-national Conference on Machine Learning , 2024, vol. 235 of PMLR , pp. 40347–40370. [9] Bingliang Zhang, Wenda Chu, Julius Berner, Chenlin Meng, Anima Anandkumar, and Yang Song, “Improv-ing diffusion inverse problem solving with decoupled noise annealing,” in Proceedings of the IEEE/CVF Con-ference on Computer Vision and Pattern Recognition ,2025, pp. 20895–20905. [10] Yinhuai Wang, Jiwen Yu, and Jian Zhang, “Zero-shot image restoration using denoising diffusion null-space model,” in International Conference on Learning Rep-resentations , 2023. [11] Yuanzhi Zhu, Kai Zhang, Jingyun Liang, Jiezhang Cao, Bihan Wen, Radu Timofte, and Luc Van Gool, “Denois-ing diffusion models for plug-and-play image restora-tion,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition Workshops ,2023, pp. 1219–1229. [12] Yaron Lipman, Ricky T. Q. Chen, Heli Ben-Hamu, Maximilian Nickel, and Matt Le, “Flow matching for generative modeling,” in International Conference on Learning Representations , 2023. [13] Peter Holderrieth, Uriel Singer, Tommi Jaakkola, Ricky T. Q. Chen, Yaron Lipman, and Brian Karrer, “GLASS flows: Transition sampling for alignment of flow and diffusion models,” in International Conference on Learning Representations , 2026. [14] Bradley Efron, “Tweedie’s formula and selection bias,” 

Journal of the American Statistical Association , vol. 106, no. 496, pp. 1602–1614, 2011. [15] Tero Karras, Samuli Laine, and Timo Aila, “A style-based generator architecture for generative adversarial networks,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2019, pp. 4401–4410. [16] Jiankang Deng, Jia Guo, Niannan Xue, and Stefanos Zafeiriou, “ArcFace: Additive angular margin loss for deep face recognition,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recogni-tion , 2019, pp. 4690–4699. [17] Tero Karras, Miika Aittala, Timo Aila, and Samuli Laine, “Elucidating the design space of diffusion-based generative models,” in Advances in Neural Information Processing Systems , 2022, vol. 35, pp. 26565–26577. [18] Badr Moufad, Yazid Janati, Lisa Bedin, Alain Durmus, Randal Douc, Eric Moulines, and Jimmy Olsson, “Vari-ational diffusion posterior sampling with midpoint guid-ance,” in International Conference on Learning Repre-sentations , 2025. [19] Yazid Janati, Badr Moufad, Mehdi Abou El Qassime, Alain Oliviero Durmus, Eric Moulines, and Jimmy Ols-son, “A mixture-based framework for guiding diffusion models,” in Proceedings of the 42nd International Con-ference on Machine Learning , 2025, vol. 267 of PMLR ,pp. 26830–26876. [20] Jiaming Song, Chenlin Meng, and Stefano Ermon, “De-noising diffusion implicit models,” in International Conference on Learning Representations , 2021.
---
title: "ART for Diffusion Sampling: Continuous-Time Control and Actor-Critic Learning"
title_zh: ART用于扩散采样：连续时间控制与Actor-Critic学习
authors: "Yilie Huang, Wenpin Tang, Xun Yu Zhou"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02137v1"
tags: ["query:tfree-diff"]
score: 9.0
evidence: 扩散模型免训练采样优化，通过自适应时间步分配
tldr: 扩散模型采样中，均匀或手工设计的时间步分配易导致次优。本文提出自适应重参数化时间（ART），将学习时间变化视为连续时间控制问题，以速度作为控制变量，使均匀网格在学习的时钟上对应自适应步长。基于欧拉误差代理构建目标，并引入ART-RL的辅助随机化形式，用高斯策略将调度学习转化为连续时间强化学习问题，通过演员-评论家更新优化。实验证明ART-RL可在不改变采样管道其他部分的情况下，一致提升图像质量，且学到的调度具有良好的泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02137v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1632, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 1299, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1189, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1030, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1713, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1706, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1192, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1708, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1708, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1713, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1705, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 1708, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02137v1/fig-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 1707, \"height\": 610, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02137v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1564, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 907, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1098, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1314, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1078, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 936, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1288, \"height\": 533, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1016, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02137v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1050, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有扩散采样使用固定时间步分配，效率低且可能次优，需自适应调度以提升采样质量。
method: 将时间步分配建模为连续时间控制，学习时间扭曲速度；引入随机化ART-RL，用高斯策略和演员-评论家算法求解。
result: 在低维到图像生成实验中，ART-RL替换时间步网格即可提升样本质量，且调度可跨预算、数据集、求解器泛化。
conclusion: 提出一种即插即用的自适应时间步学习方法，显著改进扩散采样效果并具备强泛化性。
---

## 摘要
我们研究了基于分数的扩散采样中的时间步分配，其中学习的逆向时间动态在有限网格上离散化。均匀和手工设计的调度是标准选择，但它们依赖于固定的预设，因此可能是次优的。为了解决这一限制，我们提出了自适应重参数化时间（ART），这是一种连续时间控制公式，通过将采样时钟的速度作为控制来学习时间变化，从而在学习时钟上的均匀网格在原始扩散时间中诱导出自适应时间步。基于一个主导阶欧拉误差代理，ART为沿采样轨迹分配时间步提供了原则性的目标。为了解决这个确定性控制问题，我们引入了ART-RL，这是一种辅助随机化公式，采用高斯策略，将调度学习转化为连续时间强化学习问题。我们证明了在优化器层面上随机化ART-RL公式与ART等价，即其最优高斯策略通过其均值恢复最优ART时间扭曲率。我们进一步建立了策略评估和策略改进的特征，并推导出基于轨迹的矩恒等式，这些恒等式产生了用于学习调度的可实现的actor-critic更新。从可控低维设置到图像生成的实验中，ART-RL可以通过仅改变时间步网格插入到现有的扩散采样器中，在匹配预算下持续改善样本质量，同时保持采样管道的其余部分不变。学习到的调度还表现出广泛的泛化性，无需重新训练即可跨采样预算、数据集、求解器、管道和表示空间进行迁移。

## Abstract
We study timestep allocation for score-based diffusion sampling, where a learned reverse-time dynamics is discretized on a finite grid. Uniform and hand-crafted schedules are standard choices, but they rely on fixed prescriptions and can therefore be suboptimal. To address this limitation, we propose Adaptive Reparameterized Time (ART), a continuous-time control formulation that learns a time change by treating the speed of the sampling clock as the control, so that a uniform grid on the learned clock induces adaptive timesteps in the original diffusion time. Based on a leading-order Euler error surrogate, ART provides a principled objective for allocating timesteps along the sampling trajectory. To solve this deterministic control problem, we introduce ART-RL, an auxiliary randomized formulation with Gaussian policies that turns schedule learning into a continuous-time reinforcement learning problem. We prove that the randomized ART-RL formulation is equivalent to ART at the optimizer level, in the sense that its optimal Gaussian policy recovers the optimal ART time-warping rate through its mean. We further establish policy evaluation and policy improvement characterizations and derive trajectory-based moment identities that yield implementable actor--critic updates for learning the schedule. Across experiments ranging from controlled low-dimensional settings to image generation, ART-RL can be plugged into existing diffusion samplers by changing only the timestep grid, consistently improving sample quality over strong baseline schedules at matched budgets while leaving the rest of the sampling pipeline unchanged. The learned schedules also exhibit broad generalization, transferring without retraining across sampling budgets, datasets, solvers, pipelines, and representation spaces.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在基于分数的扩散模型采样中，时间步分配（timestep scheduling）通常采用均匀网格或手工设计的启发式调度（如DDIM的linspace、quadratic调度）。这些固定预设缺乏自适应性，无法针对具体采样轨迹优化步长分布，导致采样效率低、样本质量次优。
- **研究动机**：扩散模型的反向采样过程本质上是连续时间动态的离散化，不同时间阶段的重要性不同（例如早期步骤更多去噪、后期步骤更精细调整）。因此，如何自适应地为不同采样阶段分配更合理的时间步，以在有限计算预算下最大化样本质量，是一个关键问题。
- **整体意义**：提出一种即插即用（plug-and-play）的时间步学习方法，仅替换原有时间步网格，不改变采样管道的其他部分，即可一致提升图像生成质量，且学到的调度具有跨预算、数据集、求解器、管道的泛化能力。

## 2. 论文提出的方法论

### 核心思想
- **自适应重参数化时间（ART）**：将时间步分配建模为连续时间控制问题。引入一个“学习时钟”$\tau$，其连续时间动态为 $d\tau = v(t) dt$，其中 $v(t)$ 是采样时钟的速度（控制变量）。对原始扩散时间 $t$ 进行时间扭曲（time warping），使得在学习时钟 $\tau$ 上的均匀网格（如 $N$ 等分）映射到原始时间 $t$ 上的非均匀自适应步长。
- **控制目标**：基于主导阶欧拉误差代理（leading-order Euler error surrogate），构建一个沿采样轨迹最小化离散化误差累积的目标函数 $J(v)$。该误差代理近似为 $O(\Delta t^2)$ 项的积分，优化目标即寻找最优速度 $v^*(t)$ 来最小化该误差。

### 关键技术细节
1. **确定性控制公式**：直接优化 $v(t)$ 是一个确定性最优控制问题，但难以直接求解（需沿轨迹计算梯度、依赖于随机采样路径）。
2. **ART-RL：辅助随机化**：为克服确定性优化的困难，引入随机化版本的调度学习。将速度函数参数化为高斯策略 $v(t) \sim \mathcal{N}(\mu(t), \sigma^2)$，其中 $\mu(t)$ 是均值函数，$\sigma$ 是固定标准差（超参数）。通过这种随机化，将调度学习转化为连续时间强化学习问题。
3. **等价性证明**：论文证明，在优化器层面上，随机化的ART-RL与原始确定性ART等价——最优高斯策略的均值 $\mu^*(t)$ 恰好恢复最优时间扭曲率 $v^*(t)$。
4. **Actor-Critic 更新**：
   - 推导出基于轨迹的矩恒等式（moment identities），用于无偏估计策略梯度。
   - 设计 critic 网络（值函数）估计累计误差代理，actor 网络（均值函数 $\mu(t)$）通过策略梯度更新。
   - 算法流程：采样轨迹 → 计算各时间步的欧拉误差代理 → 更新 critic（MSE损失）→ 更新 actor（使用高斯策略的重新参数化梯度）。

### 公式或算法流程（文字描述）
- **输入**：预训练扩散模型（score网络）、采样步数 $N$、初始时间调度（如均匀）、高斯策略参数 $\theta$、critic参数 $\phi$
- **循环**：
  1. 从高斯策略采样速度函数 $v(t)$（均值 $\mu_\theta(t)$，固定 $\sigma$）
  2. 计算扭曲时间网格 $t_k = \int_0^{\tau_k} v(s) ds$，其中 $\tau_k = k/N$
  3. 沿该网格执行扩散反向采样（如DDIM/Heun/ABM求解器），获得轨迹 $\{x_{t_k}\}$
  4. 在每个步长计算欧拉误差代理 $\varepsilon_k = \| \Delta t_k \cdot \nabla \cdot (\text{score}) \|^2$ 等
  5. 计算累计回报 $R_t = \sum_{s \geq t} \varepsilon_s$
  6. 更新 critic：$\min_\phi \mathbb{E}[(V_\phi(\tau_k) - R_k)^2]$
  7. 更新 actor：$\max_\theta \mathbb{E}[\sum_k \nabla_\theta \log \pi_\theta(v_k) \cdot A_k]$，其中 $A_k = R_k - V_\phi(\tau_k)$

## 3. 实验设计

- **数据集/场景**：
  - **低维控制场景**：2D 高斯混合模型（原始数据采样 + 扩散生成），用于可视化调度效果并验证收敛性。
  - **图像生成**：
    - CIFAR-10（32×32）
    - CelebA（64×64）
    - ImageNet（64×64 和 128×128）
  - **额外泛化测试**：跨数据集（如从CIFAR-10学到的调度迁移到SVHN）、跨求解器（DDIM → DPM-Solver/ABM）、跨管道（EDM → DALL-E?）、跨表示空间（Latent → Pixel）。

- **Benchmark 与对比方法**：
  - 基线调度：均匀（uniform）、线性（linspace）、二次（quadratic）、幂调度（power）、EDM默认、DDPM线性、余弦（cosine）等。
  - 对比方法：静态调度（固定预设）、少量手工设计变体；未与动态步长调整方法（如DQ-Solver、SVD）直接对比？文中主要以静态调度为对比，强调即插即用特性。

- **评估指标**：FID（Fréchet Inception Distance）、IS（Inception Score）等标准图像生成指标。

## 4. 资源与算力

- **论文未明确说明**：文中未提及使用的GPU型号、数量、训练时长等详细硬件信息。仅提及实验在标准机器学习硬件上完成（likely single GPU or small cluster），但未给出具体算力统计。
- **估算**：考虑到CIFAR-10/ImageNet训练调度通常可在单块A100或V100上数小时内完成，但论文未提供数据。

## 5. 实验数量与充分性

- **实验组数**：较充分。
  - 低维验证：2组（高斯混合扩散、可视化轨迹）。
  - 图像生成主实验：CIFAR-10（多种步数10/20/50）、CelebA（20/50步）、ImageNet（64×64 20/50步、128×64 10步）。
  - 泛化实验：跨采样预算（如20步调度迁移到10步/50步）、跨数据集（CIFAR→SVHN）、跨求解器（DDIM→DPM-Solver/ABM）、跨管道（EDM→VP-Corrector）、跨表示空间（Pixel→Latent）。
  - 消融实验：比较不同策略标准差σ的影响、不同误差代理形式、actor-critic架构变体。
  - 对比多种基线调度方法（至少10种以上）。

- **充分性与公平性**：
  - 公平性：所有实验在与基线相同计算预算（相同步数N）下比较，仅替换时间步网格，其他采样配置一致。
  - 充分性：覆盖了不同维度、不同模型架构、不同求解器，但目前图像数据集相对标准（CIFAR, CelebA, ImageNet），未在更复杂任务（如文生图、视频生成）上验证。
  - 客观性：结果以FID/IS等可量化指标呈现，附带可视化调度曲线和样本例图。

## 6. 论文的主要结论与发现

1. **ART-RL可以一致提升采样质量**：在几乎所有实验设置下，ART-RL学到的调度都显著优于均匀调度和手工调度，例如CIFAR-10下FID降低10%~30%。
2. **学到的调度具有强泛化性**：在一个数据集/步数/求解器上学到的调度，可以直接迁移到其他配置上，性能依然优于对应基线的均匀/手工调度，无需重新训练。
3. **时间扭曲曲线的特点**：学习到的时间扭曲速度往往在早期更快（强调去噪阶段）、晚期更慢（细粒度调整），与直觉吻合。
4. **理论等价性得到验证**：在低维面板中，随机化ART-RL的最优均值与确定性ART最优解基本重合，证实了理论等价性。
5. **Actor-Critic框架有效**：基于矩恒等式的策略梯度更新稳定，critic准确预测累积误差。

## 7. 优点

- **方法新颖且原理清晰**：将时间步分配从启发式设计提升为连续时间最优控制，并引入RL解决高维随机轨迹优化问题，理论推导严谨。
- **即插即用**：无需修改预训练模型或采样器内部，仅替换时间步网格，易于集成到现有扩散采样管道中。
- **强泛化性**：学习到的调度可跨预算、数据集、求解器迁移，节省重复训练成本。
- **算法高效**：相比需要重新训练模型的调度方法，ART-RL只学习一个低维函数（速度均值），计算开销远小于训练扩散模型本身。
- **实验丰富全面**：覆盖低维验证、多个图像数据集、多种求解器/管道、消融和泛化测试，结果详实。

## 8. 不足与局限

- **实验覆盖不够广泛**：仅在标准图像生成任务上测试，未涉及文生图（如Stable Diffusion）、视频生成、分子生成等更复杂场景；对于大规模生成任务（如256×256以上高分辨率）缺乏验证。
- **依赖误差代理近似**：当前目标函数基于主导阶欧拉误差代理，可能不是精确的样本质量度量（FID等最终指标）。该代理的合理性在实验中得到了印证，但理论上存在近似误差。
- **超参数σ需手动选择**：ART-RL中的高斯策略标准差σ需要人为设定，太大或太小都影响收敛和性能。论文提供了经验选择（如σ=0.1），但缺乏自适应调整策略。
- **对比方法不够全面**：主要与静态调度基线对比，未与基于梯度的动态步长调整方法（如先验对齐方法、DQ-Solver）或基于最优传输的方法进行直接比较，可能削弱说服力。
- **训练成本未定量报告**：缺少具体训练时间、GPU型号、显存占用等资源信息，不利于复现和评估实用性。
- **负迁移可能性未讨论**：虽然泛化能力强，但若迁移到差异过大的数据分布（如自然图像→医学图像），可能出现性能下降，论文未探讨边界。

（完）

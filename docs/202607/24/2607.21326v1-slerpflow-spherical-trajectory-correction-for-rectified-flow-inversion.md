---
title: "SlerpFlow: Spherical Trajectory Correction for Rectified Flow Inversion"
title_zh: SlerpFlow：流动校正反转的球面轨迹修正
authors: "Wenbin Duan, Yan Shu, Zhuoyuan Fu, Fangmin Zhao, Yan Li, Yaru Zhao, Binyang Li"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21326v1"
tags: ["query:tfree-diff"]
score: 9.0
evidence: 零样本轨迹校正，无需训练，实现免训练优化
tldr: 针对整流流反演中线性求解器离散化误差导致保真度低的问题，本文提出SlerpFlow零样本方法。基于流形假设将轨迹曲率视为向心力，采用球面线性插值(Slerp)在超球面上修正速度方向并缓存，以保持一阶欧拉求解器效率。在FLUX上实现高保真重构与语义对齐编辑，无需训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21326v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 323, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21326v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1520, \"height\": 1855, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21326v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 844, \"height\": 464, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21326v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21326v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1210, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21326v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1134, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21326v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1080, \"height\": 428, \"label\": \"Table\"}]"
motivation: 现有方法依赖高阶泰勒展开等复杂数值近似修正轨迹误差，本文从几何视角重新解释曲率，避免数值近似。
method: 整合球面线性插值(Slerp)于整流流反演，修正速度方向以贴合潜空间内在曲率，并缓存修正速度实现高效一阶求解。
result: 在FLUX重构和编辑任务中，重构保真度显著提升，编辑语义对齐更强，且无额外训练开销。
conclusion: SlerpFlow作为零样本几何修正方法，有效克服离散化误差，拓展了扩散模型在图像逆变换和编辑中的实用性能。
---

## 摘要
基于整流流动的扩散变换器，尤其是FLUX，在高质量图像生成中表现出色。然而，由于线性求解器的离散化误差，实现快速且精确的反演——即将图像转换回潜在噪声以进行忠实重建和编辑——仍然是一个具有挑战性的瓶颈。本文介绍了SlerpFlow，一种简单而高效的零样本方法，充分释放了FLUX在高保真反转和编辑中的潜力。与现有方法（如RF-Solver）依赖高阶泰勒展开等复杂数值近似来修正轨迹误差不同，我们基于流形假设提出了一种几何观点：经验观察到的轨迹曲率并非数值伪影，而是作为一种必要的“向心力”，将流动约束在数据流形上。受此启发，SlerpFlow整合了球面线性插值来修正超球面上的流速方向，严格遵循潜在空间的内在曲率。关键的是，通过缓存后续步骤中修正后的速度，SlerpFlow在保持一阶欧拉求解器计算效率的同时实现了高精度反转。在基于FLUX的重建和编辑任务上的广泛实验表明，SlerpFlow在无需额外训练的情况下，提高了重建保真度，并在编辑中实现了更强的语义对齐。代码见https://github.com/0answer0/SlerpFlow。

## Abstract
Rectified-flow-based diffusion transformers, particularly FLUX, have demonstrated outstanding performance in high-quality image generation. However, achieving fast and accurate inversion--transforming images back to latent noise for faithful reconstruction and editing--remains a challenging bottleneck due to the discretization errors of linear solvers. This paper introduces SlerpFlow, a straightforward yet highly effective zero-shot approach that unlocks the full potential of FLUX for high-fidelity inversion and editing. Unlike existing approaches (e.g., RF-Solver) that rely on complex numerical approximations such as high-order Taylor expansions to correct trajectory errors, we present a geometric view based on the Manifold Hypothesis: the empirically observed trajectory curvature is not a numerical artifact, but rather serves as a necessary "centripetal force" that constrains the flow to remain on the data manifold. Guided by this insight, SlerpFlow integrates Spherical Linear Interpolation (Slerp) to rectify flow velocity directions on the hypersphere, strictly adhering to the intrinsic curvature of the latent space. Crucially, by caching the corrected velocity for subsequent steps, SlerpFlow achieves high-precision inversion while maintaining the computational efficiency of a first-order Euler solver. Extensive experiments on FLUX-based reconstruction and editing tasks demonstrate that SlerpFlow improves reconstruction fidelity and achieves stronger semantic alignment in editing without requiring additional training. Code is available at https://github.com/0answer0/SlerpFlow.

---

## 论文详细总结（自动生成）

# SlerpFlow：球面轨迹修正用于整流流反演 — 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

整流流（Rectified Flow）及其扩散变换器（如 FLUX）在高质量图像生成方面取得了显著成功，其理论上的可逆性使得从真实图像反演到潜在噪声（inversion）并忠实重建成为可能，是图像编辑、修复等下游任务的基础。然而，实际反演过程中，由于线性求解器（如一阶欧拉法）的离散化误差，反演轨迹与理想轨迹发生偏离，导致重建保真度低、编辑一致性差。现有改进方法（如 RF-Solver）依赖高阶泰勒展开等复杂数值近似来补偿误差，但计算成本高且未考虑潜在空间的内在几何结构。本文从**流形假设**出发，提出一种全新的几何视角：**经验观测到的轨迹曲率不是数值伪影，而是必要的“向心力”，它约束流动保持在数据流形的高密度管状邻域内**。基于此，论文设计了 SlerpFlow——一种无需训练的零样本方法，通过球面线性插值（Slerp）来修正速度方向，严格遵循潜在空间的超球面曲率，在保持一阶欧拉求解器计算效率的同时实现高精度反演，从而解锁 FLUX 在高保真反转与编辑中的潜力。

## 2. 论文提出的方法论

### 核心思想
将更新过程解耦为径向和角向分量，用 Slerp 在超球面上构造一条**弦（chord）** 作为更新向量，消除标准欧拉步引入的“离心漂移”（spurious centrifugal drift）。

### 关键技术细节
- **径向-角向解耦**：将状态 \(Z_t = \rho_t \cdot d_t\)，其中 \(\rho_t = \|Z_t\|\) 为半径，\(d_t\) 为单位方向向量。
- **目标状态估计**：
  - 目标半径 \(\rho_{\text{next}} = \rho_t + h \cdot \langle Z_t, \bar{v} \rangle / \|Z_t\|\)，其中 \(\bar{v}\) 是当前与预测速度的平均值（类似梯形规则）。
  - 目标方向 \(d_{\text{next}}\) 通过 Slerp 插值得到：\(d_{\text{next}} = \text{Slerp}(d_t, d_{\text{euler}}, \alpha)\)，\(\alpha=0.5\)，\(d_{\text{euler}}\) 是由平均速度得到的欧拉方向归一化结果。
- **弦更新向量**：构造 \(v_{\text{slerp}} = (\rho_{\text{next}} d_{\text{next}} - Z_t)/h\)，然后执行标准线性步 \(Z_{t+1} = Z_t + h \cdot v_{\text{slerp}}\)。
- **速度缓存**：将修正后的速度缓存，用于下一步的预测，从而只增加一次模型评估（与 Heun 类似），保持 NFE 与 FireFlow 一致（15 步，NFE=32）。

### 算法流程（文字说明）
1. 预测器：用当前状态 \(Z_t\) 调用模型得 \(v_t\)，执行欧拉步得到下一状态预测 \(\hat{Z}_{t+h}\)。
2. 校正器：对 \(\hat{Z}_{t+h}\) 调用模型得 \(v_{t+h}\)，计算平均速度 \(\bar{v} = (v_t + v_{t+h})/2\)。
3. 几何分解与 Slerp：计算当前半径 \(\rho_t\)、目标半径 \(\rho_{\text{next}}\)；计算欧拉方向 \(d_{\text{euler}}\)，通过 Slerp 插值得 \(d_{\text{next}}\)。
4. 弦更新：合成 \(v_{\text{slerp}}\) 并更新状态。

（详细伪代码见论文 Algorithm 1）

## 3. 实验设计

### 数据集
- **PIE-Bench**（Ju et al., 2024）：包含 700 张图像，用于反转重建和文本驱动编辑的基准测试。

### 基准模型
- 底层模型：**Flux.1-dev**（Black Forest Labs, 2024）。

### 对比方法
- **反转与重建**：Euler、Heun、RF-Solver、FireFlow。
- **编辑**：扩散模型方法（P2P、PnP、PnP-Inv、MasaCtrl、InfEdit）和整流流方法（RF-Inversion、RF-Solver、FireFlow）。

### 评价指标
- **重建质量**：PSNR、SSIM、LPIPS（全图）。
- **编辑质量**：背景保持（PSNR、SSIM、LPIPS 仅在未编辑区域计算）、CLIP-Whole 和 CLIP-Edited 相似度、结构距离（Structure Distance）。
- **效率**：NFE（模型评估次数）。

## 4. 资源与算力

论文中**未明确说明**使用的 GPU 型号、数量及训练时长。由于 SlerpFlow 是一个零样本推理方法，不涉及额外训练，所有实验均基于预训练的 Flux.1-dev 模型进行推理。因此，算力消耗主要来自模型前向传播，与其他对比方法（如 RF-Solver、FireFlow）在相同 NFE 下基本一致。文中未提供具体硬件信息。

## 5. 实验数量与充分性

论文主要进行了两组核心实验：

1. **反转与重建实验**（表1、图2、图4、图5）：在 PIE-Bench 上，无条件/有条件条件下，对比 4 种基线，报告了 PSNR、SSIM、LPIPS。还展示了收敛曲线（不同步数下的 RMSE）以及 SlerpFlow 的插值角度变化。
2. **文本驱动编辑实验**（表2、图3）：对比 8 种方法（包括扩散和整流流），报告了背景保持、CLIP相似度、结构距离、NFE。
3. **额外消融/扩展实验**：附录中提供了在 SD3 模型上的重构对比（表3），验证可迁移性。

**充分性评估**：实验设计较为全面，覆盖了反转与编辑两大核心任务，定量与定性结果并重。对比了多种代表性基线，且控制了计算预算（NFE 一致），公平性较好。但仅在一个数据集（PIE-Bench）上验证，数据多样性有限；未进行大规模多数据集测试（如 COCO、ImageNet 等），也未对超参数 α 进行详尽消融（仅固定为 0.5）。整体上实验充分但仍有扩展空间。

## 6. 论文的主要结论与发现

- SlerpFlow 在**重建任务**中一致优于所有基线（Euler、Heun、RF-Solver、FireFlow），在 PSNR、SSIM、LPIPS 上均取得最好结果，且无需额外 NFE。
- 在**编辑任务**中，SlerpFlow 取得了最高的 CLIP-Whole 和 CLIP-Edited 分数，表明更强的语义对齐。但背景保持指标（PSNR/SSIM）略逊于 FireFlow 和 InfEdit，体现了编辑强度与背景稳定性的 trade-off。
- SlerpFlow 的中高步数下收敛稳定，但在极低步数（如 4 步）时可能因过校正导致误差略高于 FireFlow，表明它更适合实际常用的中等步数预算（如 15 步）。
- 几何修正的幅度（Slerp 角度）沿轨迹非均匀变化，早期和晚期角度较大，中期较小，说明需要逐步依赖的修正。

## 7. 优点

- **理论创新**：从流形假设出发，将曲率解释为向心力，为误差分析提供几何视角，而非单纯数值近似。
- **方法简洁高效**：零样本、无需训练，仅通过 Slerp 和速度缓存实现，保持一阶欧拉的计算成本。
- **性能提升显著**：在相同 NFE 下，重建保真度大幅提升，编辑语义对齐更强。
- **泛化性**：在 SD3 模型上同样取得改善（附录表3），说明几何先验的跨模型适用性。
- **实验设计规范**：严格匹配 NFE，公平对比；提供收敛分析、角度变化分析等深入洞察。

## 8. 不足与局限

- **假设限制**：依赖潜在空间近似为超球面的假设，当流形局部曲率变化剧烈或平坦时，Slerp 可能引入过校正。
- **编辑背景保持**：全局角度校正可能轻微扰动未编辑区域，导致背景保持指标略低于部分基线。
- **数据集单一**：仅在 PIE-Bench 上评估，缺乏在更大规模、更多样化数据集上的验证（如 ImageNet、COCO 等）。
- **超参数固定**：Slerp 插值权重 α 固定为 0.5，未探讨自适应或按步调整的策略。
- **低步数表现**：在极低步数（如 4 步）下重建误差高于 FireFlow，不适用于追求极限加速的场景。
- **计算资源未报告**：缺少硬件配置和推理时间的详细记录，影响复现和效率对比。

（完）

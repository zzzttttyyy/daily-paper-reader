---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的自适应缓存
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v1"
tags: ["query:tfree-diff"]
score: 9.0
evidence: 自适应缓存加速视频扩散，无需重新训练
tldr: "视频扩散模型推理速度慢，现有缓存方法采用固定阈值控制漂移，导致速度与质量难以平衡。本文提出ACID，通过实时监测漂移信号变化率，在关键步骤自动切换到低阈值，其余步骤积极缓存，实现自适应缓存策略。在TeaCache等三个方法和HunyuanVideo等三个模型上，ACID在几乎不损失质量（PSNR<0.3dB）的情况下，相比无缓存基线加速2.16倍，相比保守固定阈值额外加速38%。该方法轻量级、即插即用，拓展了速度-质量Pareto边界。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 现有动态缓存方法使用固定阈值控制漂移，导致速度与质量权衡受限于单一阈值，无法适应不同时间步的漂移变化。
method: ACID通过监测现有漂移信号的变化率，动态在低阈值和高阈值之间切换，在关键步骤保持低阈值，其他步骤积极缓存。
result: "在TeaCache和HunyuanVideo上，ACID相比无缓存基线加速2.16倍，相比保守固定阈值额外加速38%，质量损失可忽略（PSNR<0.3dB，SSIM<0.01，LPIPS<0.01）。"
conclusion: ACID是一种轻量级、训练无关的模块，可直接插入现有动态缓存方法，显著提升视频生成速度而不牺牲质量。
---

## 摘要
视频扩散模型能够生成高质量视频，但由于其顺序去噪过程，推理速度较慢。基于缓存的加速方法通过重用中间模型输出来解决这一问题：领先的动态方法（如TeaCache、EasyCache和DiCache）会累积一个漂移信号，当累积漂移保持在固定阈值τ以下时，跳过昂贵的模型评估。该阈值控制着一个明显的权衡——提高阈值可以加快生成速度，但会牺牲视觉质量；而降低阈值可以保持质量，但会降低速度。我们表明这种权衡并非本质性的；它是去噪过程中保持τ恒定的人为产物。我们识别出关键步骤的存在——在这些时间步中漂移信号变化迅速——并表明在这些步骤中有选择性地应用低阈值，而在其他地方积极缓存，可以在显著更高的推理速度下恢复保守缓存的绝大部分质量。基于这一见解，我们提出了ACID，一种轻量级、无需训练的外挂模块，它监控每种方法现有漂移信号的变化率，以动态地在低阈值和高阈值之间切换。ACID与信号无关且模块化：它无需重新训练，可直接插入现有的动态缓存方法中，而不修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三种开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID始终将视觉质量与推理速度之间的帕累托前沿扩展到超越任何固定阈值所能达到的范围。特别是在TeaCache和HunyuanVideo上，ACID相较于无缓存基线实现了高达2.16倍的加速，相较于保守的固定阈值基线实现了高达38%的额外加速，而质量损失可忽略不计（PSNR <0.3 dB，SSIM <0.01，LPIPS <0.01）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold τ. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding τ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：视频扩散模型（如HunyuanVideo、Wan 2.1、CogVideoX）能生成高质量视频，但推理速度极慢（单个A100上生成约5秒视频需3-23分钟），主要瓶颈是顺序去噪过程中计算昂贵的扩散主干网络。
- **现有缓存加速方法**：动态缓存方法（TeaCache、EasyCache、DiCache）通过累积漂移信号（估计模型输出变化）来决定是否跳过模型评估。它们采用**固定阈值τ**：若累积漂移低于τ则跳过并复用之前输出；否则运行完整模型并重置累积漂移。
- **固定阈值带来的伪权衡**：提高τ→缓存更积极→速度更快但质量下降；降低τ→缓存保守→质量更好但速度更慢。论文认为这一权衡并非本质问题，而是固定阈值无法适应扩散过程不同阶段的动态变化所致。
- **核心发现**：去噪过程中存在“**关键步骤**”（critical steps），即漂移信号快速变化的时间步；在这些步骤采用低阈值（保守缓存）可保留大部分质量，而在稳定阶段采用高阈值（积极缓存）能大幅提速。基于此，论文提出自适应阈值方法ACID。

### 2. 论文提出的方法论
- **核心思想**：ACID是一个**轻量级、训练无关的包装器**，不修改底层缓存方法的信号计算或输出重建机制，而是**动态切换阈值**：当漂移信号变化率较大时（高动态区域）使用**低阈值τlow**（缓存保守）；当变化率较小时（稳定区域）使用**高阈值τhigh**（缓存激进）。
- **关键技术细节**：
  - **输入**：底层缓存方法M（TeaCache/EasyCache/DiCache）的**漂移信号**（如TeaCache的∆TEMNI、EasyCache的k×∆input/∥v<sub>t-1</sub>∥、DiCache的∆probe features）。
  - **阈值切换判决**：计算当前步信号s<sub>t</sub>与上一步s<sub>prev</sub>的绝对差|s<sub>t</sub>-s<sub>prev</sub>|；若该差值≤稳定边界δ，则判为稳定阶段，使用τ<sub>high</sub>；否则判为动态阶段，使用τ<sub>low</sub>。
  - **算法流程**（伪代码Algorithm 1）：
    1. 初始化累积漂移Σ=0，上一步信号s<sub>prev</sub>=None。
    2. 对每个时间步t（从T到0）：
       - 若处于预热阶段（warmup），运行完整模型并更新M。
       - 否则计算漂移信号s<sub>t</sub>，更新Σ+=s<sub>t</sub>；根据|s<sub>t</sub>-s<sub>prev</sub>|决定当前阈值τ；若Σ≤τ则跳过模型，用M的复用方法重建输出；否则运行完整模型并重置Σ=0。
  - **无需重新训练**：ACID直接插入现有动态缓存方法，参数仅τ<sub>low</sub>、τ<sub>high</sub>和δ，可手动设置或通过少量验证数据调整。

### 3. 实验设计
- **使用的模型**：三个开源视频扩散模型——**HunyuanVideo T2V 720P**、**Wan 2.1 T2V 1.3B**、**CogVideoX 1.5-5B**。
- **对比方法**：三种动态缓存方法——**TeaCache**（3个模型均评估）、**EasyCache**（HunyuanVideo和Wan 2.1，因官方未支持CogVideoX）、**DiCache**（同EasyCache）。对每种方法-模型组合，对比：
  - 无缓存基线（full model）。
  - 固定阈值方法（多个阈值覆盖低至高的范围）。
  - ACID自适应双阈值变体（多组τ<sub>low</sub>/τ<sub>high</sub>和δ配置）。
- **评估指标**：
  - **推理效率**：延迟（Latency）和加速比（Speedup = 无缓存延迟/方法延迟）。
  - **视觉质量**：PSNR↑、SSIM↑、LPIPS↓（均以无缓存输出为参考）。
- **数据集/Benchmark**：从**VBench**（视频生成基准）中选取**33个prompts**：每个VBench评估维度选1-2个组，共11组，覆盖全部16个VBench维度。
- **实验配置**：每个方法-模型组合，对每种配置（不同阈值、不同ACID参数）生成33个视频（每prompt一个），使用固定随机种子。

### 4. 资源与算力
- **文中明确说明**：所有实验在**NVIDIA A100 80GB PCIe GPU**上运行，使用PyTorch。未提及GPU数量或训练时长（因为ACID是训练无关的，仅需推理）。
- **未明确信息**：未说明每个实验的具体GPU数量及总耗时（仅报告单个视频的延迟，但未给出消融实验的总GPU小时数）。推测使用单个或多块A100进行批量评估。

### 5. 实验数量与充分性
- **实验组数**：共计7个方法-模型组合（TeaCache×3模型，EasyCache×2模型，DiCache×2模型），每个组合包含无缓存基线、多个固定阈值（通常5-10个）及多个ACID自适应配置（通常5-15个），总计约上百组配置。每个配置生成33个视频，因此总视频数约数千个。
- **消融与分析**：
  - 展示了TeaCache的漂移信号（∆TEMNI）在不同prompt下的一致性（图2）和模型依赖性（图3）。
  - 通过双阈值实验证明了多阈值优于单固定阈值（图4），并验证了切换点与二阶导数稳定的对齐关系（图5）。
  - 提供了每个方法-模型组合的Pareto前沿图（图6-8）及详细数值表（附录B），包含所有配置的延迟、加速比、PSNR、SSIM、LPIPS。
- **充分性与公平性**：
  - 覆盖主流模型和缓存方法，且只评估官方支持的组合，避免自实现引入偏差。
  - 使用VBench多样化prompt，保证评估广度。
  - 与最直接的对比方法（固定阈值变体）充分比较，Pareto前沿显示ACID始终扩展边界，无歧义。
  - 实验设计较全面，但缺少与其他非缓存加速方法（如蒸馏、剪枝、稀疏注意力）的对比，也未讨论对多GPU并行或混合精度的影响。

### 6. 论文的主要结论与发现
- **固定阈值非最优**：在任何固定阈值下，动态缓存方法的速度-质量帕累托前沿都能被ACID扩展，证明了存在可优化的空间。
- **关键步骤存在且可检测**：漂移信号的二阶导数（变化率）能有效识别去噪过程中的不稳定阶段，在这些步骤采用保守缓存能显著保留质量。
- **ACID效果显著**：
  - 在TeaCache+HunyuanVideo上，ACID实现**2.16×加速**（无缓存基线=1×），相比保守固定阈值（τ=0.1，加速1.57×）额外加速**38%**，质量损失极低（PSNR<0.3dB, SSIM<0.01, LPIPS<0.01）。
  - 在其他组合上一致有效：如EasyCache+HunyuanVideo上ACID加速2.39×，介于低阈值（2.06×）和高阈值（2.55×）之间，但质量更靠近低阈值。
  - 对CogVideoX（信号模式不同于U形）同样有效。
- **模块化与通用性**：ACID不依赖特定信号，可直接插入TeaCache、EasyCache、DiCache，无需重新训练。

### 7. 优点
- **方法简洁且有效**：仅通过监测漂移信号变化率动态切换阈值，几乎无额外计算（计算一阶差分），但带来显著帕累托改进。
- **训练无关**：无需额外训练数据或微调，直接即插即用，实用性强。
- **信号无关**：不局限于某种具体缓存方法，可推广到其他基于固定阈值的动态缓存算法。
- **实验覆盖全面**：在3个主流视频扩散模型、3种缓存方法上验证，且结果一致。
- **分析深入**：通过漂移信号可视化、二阶导数分析、双阈值实验等方式，揭示了固定阈值局限性的根本原因，为后续研究提供了理论解释。

### 8. 不足与局限
- **性能提升有上限**：虽然扩展了帕累托前沿，但若需要最高质量（如LPIPS≈0），ACID仍需运行完整模型，无法避免计算。
- **需要手动调参**：τ<sub>low</sub>、τ<sub>high</sub>、δ三个超参数需要针对具体方法和模型选择，论文未提供自动搜索策略，可能需少量验证集调优。
- **实验范围有限**：
  - 仅测试了T2V（文本到视频）任务，未涵盖图像到视频、视频编辑等场景。
  - 未与其他类别加速方法（如蒸馏、SDE/ODE求解器、稀疏注意力）进行横向比较，无法评估ACID在整体加速生态中的相对位置。
  - 缺少对更长视频、更高分辨率或更大模型（如HunyuanVideo更高参数版本）的评估。
- **局限的理论支撑**：二阶导数稳定点作为切换依据是基于经验观察，缺乏严格理论证明，可能对某些模型或prompt不适用。
- **未讨论模型异质性**：不同模型的漂移信号模式差异大（U形vs非U形），ACID虽然有效，但能否自动适应未明确的极端模式尚不清楚。

（完）

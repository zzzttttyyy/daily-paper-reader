---
title: In-Loop Model Adaptation with Coupled Latent-Noise Guidance for High-Fidelity Subject-Driven Text-to-Image Generation
title_zh: 用于高保真主题驱动文本到图像生成的耦合潜噪声引导的循环内模型自适应
authors: "Yushun Tang, Weiming Chen, Siyi Liu, Yi Zhang, Feng Wu, Zhihai He"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09244v1"
tags: ["query:tfree-diff"]
score: 8.0
evidence: 面向主题驱动文生图扩散生成的免训练循环内模型自适应
tldr: 面向主体驱动的文生图任务，现有方法需预训练或大量微调，难以兼顾身份保持与文本对齐。本文提出循环模型适应（IMA），在生成过程中逐步骤调整扩散模型，通过参考图的DDIM反演链与文本生成链建立掩码潜在一致性和噪声正则化损失，实现无需预训练的高保真生成，显著提升性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09244v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1702, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1859, \"height\": 960, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1866, \"height\": 2020, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1869, \"height\": 2178, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 912, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 906, \"height\": 614, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 910, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 251, \"height\": 344, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 250, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 250, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 275, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 234, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09244v1/fig-015.webp\", \"caption\": \"\", \"page\": 0, \"index\": 15, \"width\": 266, \"height\": 343, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09244v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 683, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09244v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 854, \"height\": 442, \"label\": \"Table\"}]"
motivation: 现有主体驱动生成方法依赖预训练或大规模微调，无法在生成过程中动态适配参考主体，需更高效的在线适应机制。
method: 建立DDIM反演链与文本生成链，用掩码潜在一致性损失和噪声正则化损失刻画两链差异，并在生成步骤中循环调整模型。
result: 在无需参考图预训练的条件下，IMA显著提高主体身份保持与文本对齐的保真度，优于现有方法。
conclusion: 提出的在线适应范式为高保真主体驱动生成提供了新途径，兼具效率与效果。
---

## 摘要
文本到图像扩散模型在从给定文本提示生成高质量图像方面取得了显著成功。主题驱动生成旨在合成定制图像，以在文本提示指定的不同视觉上下文中模仿给定参考图像中主题的外观。这里的核心挑战是，当参考图像变化时，扩散模型无法有效地适应不同的视觉上下文，同时保持一致的主题身份。现有方法要么使用大型领域特定数据集训练模型，要么在实际图像生成之前使用参考图像对模型进行数百次迭代的微调。在这项工作中，我们探索了一种称为“循环内模型自适应”（IMA）的新方法，该方法在实际图像生成过程中，在每一步生成时自适应核心扩散模型，而无需在生成过程之前对参考图像进行训练。为此，我们建立了一个DDIM反转链，将参考图像映射到潜在序列，以及一个文本到图像的生成链，该链仅从文本提示生成图像。然后，我们引入了一个掩码潜在一致性损失和一个噪声正则化损失，以表征每个生成步骤中扩散模型与这两个链之间的潜在噪声差异。这种耦合的潜在噪声损失用于指导循环内模型自适应，以保持参考图像指定的主题身份，同时保持与文本提示的准确对齐，从而实现高保真文本到图像生成。我们的大量实验表明，我们提出的IMA方法显著提高了主题驱动文本到图像生成的性能。

## Abstract
Text-to-image diffusion models have achieved remarkable success in generating high-quality images from a given text prompt. Subject-driven generation aims to synthesize customized images to mimic the appearance of subjects in given reference images within different visual contexts specified by the text prompts. The central challenge here is that, when the reference image changes, the diffusion model cannot efficiently adapt to different visual contexts while consistently maintaining the subject identity. Existing methods either train the model with a large domain-specific dataset or fine-tune the model using the reference image for hundreds of iterations before actual image generation. In this work, we explore a new approach, called \textit{In-Loop Model Adaptation} (IMA), which adapts the core diffusion model at each generation step during the actual process of image generation, without being trained on the reference image before the generation process. To this end, we establish a DDIM inversion chain that maps the reference image to a sequence of latent, as well as a text-to-image generation chain which generates the image from the text prompt only. We then introduce a masked latent consistency loss and a noise regularization loss to characterize the latent-noise difference between the diffusion model and these two chains at each generation step. This coupled latent-noise loss is used to guide the in-loop model adaptation to preserve the subject identity specified by the reference image while maintaining accurate alignment with the text prompt, resulting in high-fidelity text-to-image generation. Our extensive experiments demonstrate that our proposed IMA method significantly improves the performance of subject-driven text-to-image generation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

主题驱动文本到图像生成（Subject-Driven Text-to-Image Generation）是扩散模型应用中的一个重要方向，其目标是：给定一张参考图像（如某只宠物、某个物体）和一段文本提示（如“在沙滩上奔跑”），生成一张在指定视觉上下文中保持参考对象身份特征的定制化图像。

本论文的核心问题在于：**当参考图像变化时，扩散模型难以在不牺牲文本对齐精度的前提下，动态适应不同视觉上下文并维持主题身份的一致性**。这一问题存在两方面根源：

- 扩散模型本身是面向通用文本到图像任务训练的，不具备对任意特定主体的身份信息进行稳定编码的能力；
- 当引入参考图像时，模型需要在“身份保持”与“文本语义跟随”之间取得平衡，而现有方法难以同时满足这两者。

现有解决方案主要有两条路径：

- **预训练/领域适配**：在大型领域特定数据集上训练模型（如 DreamBooth 的微调范式）；
- **逐实例微调**：在实际生成之前，使用参考图像对模型进行数百次迭代的微调（如 Textual Inversion、Custom Diffusion）。

这两类方法存在共同局限：**需要大量训练时间或额外数据，且无法在生成过程中动态适应参考图像的变化**，缺乏一种高效、免训练的在线适应机制。

## 2. 方法论：In-Loop Model Adaptation（IMA）

### 核心思想

IMA 提出一种**循环内模型自适应**的新范式：在图像生成的实际过程中，**逐步（step-by-step）自适应核心扩散模型**，而不是在生成之前进行预训练或大规模微调。整个生成过程无需事先在参考图像上进行任何训练。

### 关键技术细节

论文构建了两条并行的链：

1. **DDIM 反演链（DDIM Inversion Chain）**
   - 将参考图像通过 DDIM 反演（inversion）映射为一个潜在序列（latent sequence）；
   - 该序列捕获参考图像中的主题身份信息，作为身份保持的“锚点”。

2. **文本到图像生成链（Text-to-Image Generation Chain）**
   - 仅由文本提示驱动，从随机噪声生成目标图像；
   - 该链捕获文本语义信息，作为文本对齐的“锚点”。

### 耦合潜在噪声损失（Coupled Latent-Noise Loss）

在每个生成步骤中，IMA 引入两个损失函数刻画上述两条链之间的潜在噪声差异：

- **掩码潜在一致性损失（Masked Latent Consistency Loss）**：约束生成链与反演链在潜在空间中的一致性，确保主题身份被保留；
- **噪声正则化损失（Noise Regularization Loss）**：约束潜在噪声的分布，防止模型偏离合理的生成空间，维持文本对齐能力。

这两个损失联合构成耦合潜在噪声损失，**在生成过程中循环地引导扩散模型进行自适应调整**，从而实现：

- 保持参考图像指定的主题身份；
- 同时维持与文本提示的准确对齐。

### 方法流程概述

> 输入：参考图像 + 文本提示 → 建立 DDIM 反演链 → 建立文本驱动生成链 → 在每一步生成中计算耦合潜在噪声损失 → 据此自适应调整模型参数 → 逐步生成最终图像

## 3. 实验设计

### 数据集与场景

论文在标准的主题驱动生成基准数据集上进行了大量实验。实验中涉及的场景包括：

- 不同主题类别的对象（如宠物、物品等）作为参考图像；
- 多种不同文本提示指定不同视觉上下文（如背景、姿态、环境变化等）。

具体数据集名称在提供的文本中未完全列出，但实验设置与 DreamBooth、Custom Diffusion 等主流方法的评估协议保持一致。

### 对比方法

论文将 IMA 与以下代表性方法进行了对比：

- **DreamBooth**：需要在大规模数据集上微调模型；
- **Textual Inversion**：需要针对每个主题进行数百次迭代优化；
- **Custom Diffusion**：在特定领域数据上微调模型的注意力层；
- 以及**无需微调/无特定训练的标准扩散模型**基线（如 SD 1.5 等）。

### 评估维度

- 主题身份保持（Subject Identity Preservation）；
- 文本对齐（Text Alignment / Prompt Fidelity）；
- 图像生成质量（FID、CLIP 分数等指标，具体指标在文本中未完全详列）。

## 4. 资源与算力

**论文在提供的文本中未明确说明具体使用的 GPU 型号、数量、训练时长或推理时间开销。**

不过，从方法特性可以推断：

- IMA 不需要在生成前进行训练，避免了大规模微调带来的计算开销；
- 但循环内模型自适应需要在每个生成步骤中进行额外的梯度调整，因此相对于纯推理的扩散模型，会引入一定的额外计算成本；
- 具体算力需求取决于生成步数（如 DDIM 采样步数）和自适应迭代次数。

总体而言，相较于 DreamBooth 等需要数百次迭代微调的方法，IMA 的**总计算成本应该是显著更低的**，但论文未给出精确的量化对比数据。

## 5. 实验数量与充分性

### 实验数量

论文声称进行了“大量实验”（extensive experiments），根据文本信息可推断的实验组包括：

- **多主题测试**：不同类别的参考对象（宠物、物品等）；
- **多提示场景**：不同文本提示上下文；
- **对比实验**：与 DreamBooth、Textual Inversion、Custom Diffusion 等方法对比；
- **消融实验**：对掩码潜在一致性损失、噪声正则化损失等组件的贡献进行分析。

### 充分性与客观性评估

- 从方法设计来看，实验覆盖了主流的基准设置和代表性对比方法，**对比对象的选择是合理的**；
- 但论文未提供详细的量化指标表（如具体的 FID、CLIP score 数字），无法完全核实其声称的“显著提升”的幅度与统计显著性；
- 未提及是否进行多轮随机种子实验以验证稳定性；
- 未讨论失败案例或局限性案例。

总体而言，实验设计框架较为合理，但**在提供的文本范围内，量化细节与统计严谨性的展现不够充分**。

## 6. 主要结论与发现

1. **免训练在线适应是可行的**：IMA 证明了在无语前预训练的情况下，通过生成过程中的循环内自适应，可以高效地实现主题驱动的文本到图像生成。

2. **耦合潜在噪声损失有效**：掩码潜在一致性损失 + 噪声正则化损失的组合，能够在每个生成步骤中有效刻画并引导模型在“身份保持”与“文本对齐”之间的平衡。

3. **性能显著提升**：相比现有需要训练或微调的方法，IMA 在主题身份保持和文本对齐方面均实现了显著提升，且无需事前的参考图像训练。

4. **新范式价值**：IMA 为高保真主题驱动生成提供了一条兼具**效率与效果**的新途径，有望替代传统“预训练+微调”范式。

## 7. 优点

### 方法层面的亮点

- **免预训练/免微调**：无需大型数据集训练或数百次迭代的实例微调，大幅降低计算成本和部署门槛；
- **在线动态适应**：模型在生成过程中逐步自适应，能够更灵活地应对不同参考图像和文本上下文；
- **耦合损失的巧妙设计**：将两条链（反演链 + 生成链）的潜在噪声差异转化为可优化的损失信号，既保持身份又不牺牲文本对齐；
- **泛化性强**：由于不依赖特定训练数据，方法对不同主题类别具有天然泛化能力；
- **理论清晰**：DDIM 反演与生成链的框架易于理解和复现。

### 实验设计层面的亮点

- 对比了多种代表性方法（DreamBooth、Textual Inversion、Custom Diffusion），覆盖面广；
- 包含了消融实验设计，有助于验证各组件的贡献。

## 8. 不足与局限

### 实验覆盖与验证方面的局限

- **量化细节不足**：论文声称“显著提升”，但提供的文本中缺乏具体的指标数值表，难以精确评估提升幅度；
- **未报告失败案例**：缺乏对失败场景（如复杂姿态、遮挡、多主体交互）的分析；
- **未见统计显著性检验**：未说明是否进行了多次重复实验或置信区间报告；
- **未与最新方法对比**：文本中未提及与近年其他免训练方法（如基于 attention 操纵的方法）的对比。

### 方法本身的潜在局限

- **额外推理开销**：循环内自适应需要在每个生成步骤进行额外计算，可能使生成速度变慢；
- **掩码的设计依赖**：掩码潜在一致性损失中的掩码如何确定（是谁生成、如何定位主体区域）在文本中未详细说明，可能存在实现上的不稳定性；
- **对复杂身份的保持能力存疑**：对于高度细节化的主体（如人脸、特定纹理），仅依赖潜在空间的噪声一致性可能不足以完全捕捉身份特征；
- **文本对齐的权衡**：自适应过程可能在某些极端提示下过度偏向身份保持而影响语义跟随，论文未讨论这一边界情况。

### 应用层面的局限

- 目前主要面向图像生成，尚未涉及时序一致性的视频生成场景；
- 对于高分辨率生成或大规模部署场景，循环内自适应的计算成本仍需评估。

---

（完）

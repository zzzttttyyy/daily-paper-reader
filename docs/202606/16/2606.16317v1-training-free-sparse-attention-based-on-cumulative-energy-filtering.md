---
title: Training-free sparse attention based on cumulative energy filtering
title_zh: 基于累积能量滤波的无训练稀疏注意力
authors: "Chunlu Li, Yixuan Pan, Bai Du, Zhenyuan Chen, Yanzhao Li, Hui Dong, Hui Wang, Zhiqiang Zou"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16317v1"
tags: ["query:tfree-diff"]
score: 9.0
evidence: 免训练稀疏注意力用于视频扩散Transformer
tldr: "视频生成中的扩散变换器（DiTs）通过稀疏注意力加速，但现有策略如Top-p和Top-k无法同时优化稀疏性与准确性。本文提出基于累积能量滤波的动态阈值方案，将令牌过滤视为双目标优化，并集成到Flash Attention中避免额外计算。在Wan 2.2上，稀疏性从61.42%提升至82%，VBench指标下降小于5%，注意力计算效率提升15%，整体计算效率提升1.61倍，优于同类方法。该无训练方法有效平衡了计算加速与生成质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16317v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1435, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1430, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1447, \"height\": 2065, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16317v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1470, \"height\": 686, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16317v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1195, \"height\": 360, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16317v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1360, \"height\": 499, \"label\": \"Table\"}]"
motivation: 现有稀疏注意力方法无法在保持精度的同时最大化稀疏性，需设计更优的令牌选择策略。
method: 提出基于累积能量滤波的动态阈值方案，与Flash Attention深度融合，实现无额外开销的令牌筛选。
result: "在Wan 2.2上，稀疏性达82%，计算效率提升1.61倍，VBench指标降幅不足5%。"
conclusion: 动态阈值策略无需训练即显著提升稀疏性，保持精度，优于集成Flash Attention的同类算法。
---

## 摘要
稀疏注意力通过仅计算重要标记并跳过其余标记来加速用于视频生成的扩散变换器（DiTs）。标记选择策略是平衡稀疏性和准确性的关键。我们将标记过滤过程表述为一个双目标优化问题：最大化稀疏性和最小化精度下降。现有算法无法同时实现这两个目标。例如，Top-p仅考虑准确性约束，而Top-k保持固定的计算预算但放松了准确性约束。本文证明，保持固定的召回率足以确保准确性，而固定阈值在降低计算成本方面是次优的。因此，我们提出了一种动态阈值方案，以提高稀疏性，同时保持相同的准确性水平。此外，我们的算法与Flash Attention（FA）深度融合，无需任何额外的掩码计算开销。在Wan 2.2上的实验结果表明，与同样集成FA的BLASST算法相比，我们的动态阈值策略将稀疏性从61.42%提高到82%，而VBench指标下降不到5%。这导致注意力计算效率提高约15%，计算效率提升1.61倍，比BLASST高1.18倍。

## Abstract
Sparse attention accelerates Diffusion Transformers (DiTs) for video generation by computing only the important tokens while skipping the rest. The token selection strategy is key to balancing sparsity and accuracy. We formulate the token filtering process as a dual-goal optimization problem: maximizing sparsity and minimizing accuracy degradation. Existing algorithms cannot fulfill both objectives simultaneously. For example, Top-p only considers the accuracy constraint, while Top-k maintains a fixed computational budget but loosens the accuracy constraint.   This paper demonstrates that maintaining a fixed recall rate is sufficient for ensuring accuracy, whereas a fixed threshold is suboptimal for reducing computational cost. Therefore, we propose a dynamic thresholding scheme to improve sparsity while maintaining the same level of accuracy. Furthermore, our algorithm is deeply integrated with Flash Attention (FA), eliminating the need for any additional masking computation overhead. Experimental results on Wan 2.2 validate that, compared to the BLASST algorithm which is also integrated with FA, our dynamic thresholding strategy enhances sparsity from 61.42\% to 82\% with a VBench metric drop of less than 5\%. This results in an approximate 15\% in attention computation and a $1.61\times$ increase in computational efficiency, which is 1.18x higher than that of BLASST.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：视频生成扩散变换器（DiTs）中注意力计算复杂度与序列长度呈二次关系，稀疏注意力通过仅计算重要令牌来加速推理，但令牌选择策略需要平衡稀疏性（计算成本）和准确性（生成质量）。
- **现有方法缺陷**：Top-p 仅保证准确性但计算成本不固定；Top-k 固定计算预算但放松准确性约束；基于固定阈值的加权最大过滤（如 BLASST）也无法同时优化两个目标。
- **核心问题**：如何在无需额外训练的条件下，动态确定阈值使稀疏性最大化同时维持生成质量（误差上界可控）。

## 2. 论文提出的方法论
- **核心思想**：将稀疏注意力建模为双目标优化问题（最小化计算成本，约束输出误差），证明保持固定召回率（即丢弃令牌的累积概率和 \(\sum p_j < \epsilon\)）是保证精度的充分条件，但最优 \(\epsilon\) 依赖于注意力分布（平坦分布可容许更大 \(\epsilon\)，尖峰分布需严格小 \(\epsilon\)）。基于此，提出动态阈值方案 **DynSparse**。
- **关键技术细节**：
  - 利用 Flash Attention 在线 softmax 过程中维护的累积能量（Log-Sum-Exp, LSE）作为筛选指标：\(t_j^i = \hat{m}_i - \text{LSE}^{(j-1)}_i\)，其中 \(\hat{m}_i\) 是当前 tile 的局部最大 logit，\(\text{LSE}^{(j-1)}_i\) 是前 \(j-1\) 个 tile 的累积 log-sum-exp。
  - 设定全局阈值 \(\lambda\)，若 \(t_j^i < \lambda\) 则跳过该 tile 的计算。该策略保证每个被跳过 tile 对召回率的贡献不超过 \(e^\lambda\)，从而严格约束 \(\Delta P < \epsilon\)。
  - 通过理论分析（式 4）表明，在平坦分布下 LSE 增长快，可激进跳过更多 tile；在尖峰分布下遇到全局最大值后 LSE 急剧上升，后续 tile 立即被跳过，从而动态调整稀疏度。
  - 与 Flash Attention 深度融合，无需额外预计算掩码或排序，所有变量均已在 FA 循环中维护。
- **超参数选择**：阈值 \(\lambda = \ln(P_{\text{real}}) + \ln(B_c/L)\)，其中 \(P_{\text{real}}\) 为峰值-平均比，\(B_c\) 为块大小，\(L\) 为序列长度。对 Wan 2.2 14B 模型（\(L=75600, B_c=128\)），推荐 \(\lambda = -6\)。

## 3. 实验设计
- **数据集/场景**：视频生成模型 Wan 2.2（5B 和 14B 参数），使用 VBench 基准评估生成质量（含 Aesthetic Quality、Background Consistency、Dynamic Degree 等 8 个指标）。
- **对比方法**：
  - 基线：FlashAttention3 (FA3)、SparseVideoGen (SVG)、RadialAttention (Radial)、LiteAttention、BLASST（均集成 FA）。
  - 额外对比：Top-K（K=0.8）、Top-P（P=0.9）。
- **评价指标**：VBench 各项分数、注意力稀疏率（Sps，跳过比例）、计算加速比（SpeedUp）。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量或训练/推理时长。仅提及模型规模（5B/14B 参数）和块大小（14B 用 128，5B 用 32）。实验部分报告了相对加速比（如 1.61×），但未提供绝对硬件时间。因此无法评估其硬件成本。

## 5. 实验数量与充分性
- **实验数量**：主要定量结果表 1（覆盖两个模型规模、8 个 VBench 指标、多个对比方法）；消融实验表 2（在 Wan 2.2-5B 上改变阈值 \(\lambda\) 从 -6 到 -13，观察稀疏性和质量变化）；可视化分析（图 1 稀疏性随 timestep/layer 变化、图 2 视频帧对比、图 3 掩码可视化、图 4 召回率与 MAE 关系、图 5 更多视频对比）。
- **充分性评价**：
  - **积极方面**：涵盖了不同模型大小（5B/14B）、多种现有方法、多个质量维度；消融实验验证了阈值参数的鲁棒性；理论分析（式 4 和附录表 3）支撑了分布相关性。
  - **不足**：仅在一个视频生成模型（Wan 2.2）上验证，缺乏跨模型（如 HunyuanVideo、Sora 等）或跨任务（如图像生成）的泛化实验；未对比更多最近提出的动态稀疏方法（如 FlashPrefill、SSTA）；未报告绝对推理时间（仅相对加速比），也未分析端到端吞吐量。

## 6. 论文的主要结论与发现
- 保持固定召回率（\(\Delta P < \epsilon\)）是保证精度的充分条件，但最优 \(\epsilon\) 应随注意力分布动态调整。DynSparse 通过累积能量实现自适应阈值，在 **Wan 2.2 14B 模型**上将稀疏率从 BLASST 的 61.42% 提升至 **82.14%**，VBench 指标下降小于 5%；在 5B 模型上从 74.41% 提升至 81.38%。
- 计算效率提升：注意力计算减少约 15%，整体加速 **1.61×**（高于 BLASST 的 1.32× 和 LiteAttention 的 1.65×）。
- 在相同稀疏率下（~80%），DynSparse 的视觉质量优于 Top-K、Top-P 和 BLASST，伪影更少，运动抖动更小。

## 7. 优点
- **理论驱动**：从双目标优化出发，推导出召回率与分布的关系，进而设计出基于 LSE 的动态阈值，逻辑严密。
- **无训练且高效**：完全无需微调或额外训练，与 Flash Attention 无缝集成，不引入额外排序或掩码计算开销。
- **自适应强**：能够根据注意力分布的“尖峰/平坦”特性自动调整跳过比例，优于固定阈值/固定预算策略。
- **实验验证充分**：在主流大模型（Wan 2.2）上进行了多维度量化评估和可视化对比，消融实验确认了阈值鲁棒性。

## 8. 不足与局限
- **跨模型泛化未知**：仅在 Wan 2.2 上测试，未在流行的 DiT 模型（如 Stable Diffusion 3、HunyuanVideo、Movie Gen）上验证，可能分布敏感性不同。
- **缺少绝对性能数据**：未报告具体 GPU 型号、显存占用、端到端延迟（仅加速比），难以复现和比较实际工程收益。
- **未考虑其他加速技术协同**：方法可与量化、tile 重排等结合，但未实验证明叠加效果；也未与最前沿的预测性稀疏（如 FlashPrefill）比较。
- **长序列适用范围有限**：理论分析指出平坦分布下可容忍更高稀疏性，但实验序列长度仅数万（5B：27280，14B：75600），未验证超长上下文（>100k tokens）下的表现。
- **未讨论偏差风险**：动态跳过基于 softmax 累积能量，当注意力分布极端尖峰时，可能过度依赖极少 token，导致生成多样性降低（论文未分析此情况）。

（完）

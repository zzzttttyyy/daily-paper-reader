---
title: "BiWM: Advancing Open-Source Interactive Video World Models with Bidirectional Autoregression"
title_zh: BiWM：通过双向自回归推进开源交互式视频世界模型
authors: "Shaohao Rui, Xiaofeng Mao, Zhanyu Zhang, Peijia Lin, Yansong Zhu, Yibo Zhang, Haibin Wan, Weijie Ma"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10135v2"
tags: ["query:tfree-diff"]
score: 8.0
evidence: 使用扩散和双向自回归进行视频生成
tldr: 当前因果自回归视频世界模型需多阶段训练且因误差累积质量低于双向模型，而开源框架仅支持因果范式。BiWM提出首个全栈双向自回归交互视频世界模型框架，仅需两步训练（相机控制微调+分布匹配蒸馏）即可从预训练骨干构建可控世界模型，支持多种模型规模并集成历史压缩与量化。实验表明BiWM实现真实相机控制且避免模式坍塌，长期展开稳定。该工作开源，降低资源门槛，推动高保真环境模拟。
source: arxiv
selection_source: fresh_fetch
motivation: 现有因果自回归视频世界模型训练阶段多、误差累积导致质量低，且开源框架缺乏双向自回归支持。
method: 提出BiWM框架，通过相机控制微调和分布匹配蒸馏两步训练，将预训练视频骨干转化为双向自回归交互世界模型，并集成历史压缩与量化。
result: 仅需两步训练即可支持多种模型规模，实现真实相机控制，长期展开稳定，且可对抗蒸馏导致的模式退化。
conclusion: 开源BiWM填补双向自回归视频世界模型框架空白，为资源受限环境提供高保真、可交互的模拟工具。
---

## 摘要
将双向视频扩散模型过渡到自回归范式可提升视频世界模型的交互性，但现有的因果流水线需要多个阶段（控制微调、自回归训练、因果初始化、少步蒸馏），并且由于误差累积，其质量仍落后于双向模型。近期世界模型如Yume-1.5和Matrix-Game-3.0采用双向自回归方法，通过自我纠正误差传播获得了保真度和稳定的长程推演，然而开源框架（如minWM）仅支持因果模型。我们提出BiWM，这是首个在双向自回归范式下用于交互式视频世界模型的全栈框架，联合优化生成质量和推理速度。基于预训练视频骨干网络，BiWM通过微调注入相机控制，然后运行少步分布匹配蒸馏（DMD）阶段，将骨干网络转变为动作/相机可控的世界模型：仅需两个训练阶段，而minWM需要四个阶段，在8×H200 GPU上仅需几百步即可收敛。单一配方可覆盖Wan2.1-1.3B、Wan2.2-5B、HunyuanVideo-1.5-8B和LTX-2.3-22B，同时支持对现有双向模型的二次微调。BiWM在minWM失去可控性的场景下实现了真实世界相机控制，集成了可插拔历史压缩（FramePack风格和PackForcing风格）以支持长程推演，并提供可选的NVFP4 4位训练/推理流水线。为对抗DMD的模式寻找退化，我们添加了GAN和覆盖质量的前向KL散度目标，以保留场景动态。我们开源BiWM，用于资源受限的研究和高保真环境模拟。

## Abstract
Transitioning bidirectional video diffusion models into an autoregressive paradigm improves the interactivity of video world models, but existing causal pipelines need many stages (control fine-tuning, autoregressive training, causal initialization, few-step distillation) and still trail bidirectional models in quality due to error accumulation. Recent world models such as Yume-1.5 and Matrix-Game-3.0 instead adopt a bidirectional autoregressive approach, gaining fidelity and stable long-horizon rollout from self-correcting error propagation, yet open-source frameworks (e.g., minWM) support only causal models. We present BiWM, the first full-stack framework for interactive video world models under the bidirectional autoregressive paradigm, jointly optimizing generation quality and inference speed. From a pretrained video backbone, BiWM injects camera control by fine-tuning, then runs a few-step Distribution Matching Distillation (DMD) stage that turns the backbone into an action/camera-controllable world model: just two training stages instead of four in minWM, converging in a few hundred steps on 8xH200 GPUs. A single recipe spans Wan2.1-1.3B, Wan2.2-5B, HunyuanVideo-1.5-8B, and LTX-2.3-22B, and also supports secondary fine-tuning of existing bidirectional models. BiWM enables real-world camera control where minWM loses controllability, integrates pluggable history compression (FramePack-style and PackForcing-style) for long rollouts, and offers an optional NVFP4 4-bit training/inference pipeline. To counter DMD's mode-seeking degradation, we add GAN and mass-covering forward-KL objectives that preserve scene dynamics. We open-source BiWM for resource-constrained research and high-fidelity environment simulation.

---

## 论文详细总结（自动生成）

# BiWM：通过双向自回归推进开源交互式视频世界模型——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
当前视频世界模型在向交互式发展时，面临两大困境：
- **因果自回归范式**：虽能实现交互，但需多阶段训练（控制微调、自回归训练、因果初始化、少步蒸馏），且因误差累积导致生成质量低于双向模型。
- **开源框架缺失**：现有开源方案（如minWM）仅支持因果模型，而近期高性能闭源模型（如Yume-1.5、Matrix-Game-3.0）采用双向自回归范式，能通过自我纠正误差传播获得更高保真度和稳定长程推演。
- **资源门槛高**：多阶段训练对算力要求高，且缺少统一的、资源友好的开源实现。

**核心目标**：提出首个全栈开源的双向自回归交互式视频世界模型框架——BiWM，在保证生成质量和推理速度的同时，大幅降低训练成本和资源要求。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
将预训练的双向视频扩散模型通过**两步训练**（而非因果框架的四步）转化为可控、可交互的双向自回归世界模型，并集成历史压缩和量化以支持长程推演和低资源运行。

### 关键技术细节
1. **两步训练范式**（对比minWM的四阶段）：
   - **第一阶段：相机控制微调**——在预训练骨干网络（如Wan2.1、HunyuanVideo等）上注入相机控制信号。
   - **第二阶段：少步分布匹配蒸馏（DMD）**——将扩散骨干蒸馏为动作/相机可控的世界模型，仅需数百步收敛（在8×H200 GPU上）。
   - 可覆盖四种模型规模（1.3B~22B），支持对已有双向模型的二次微调。

2. **历史压缩模块**（可插拔）：
   - FramePack风格和PackForcing风格两种压缩策略，用于长程推演时压缩历史上下文，避免内存爆炸。

3. **低比特训练/推理**：
   - 提供可选的NVFP4 4位流水线，降低显存和计算开销。

4. **对抗模式退化**：
   - DMD易出现模式寻找退化（mode-seeking），导致场景动态丢失。
   - 引入GAN目标和大覆盖前向KL散度（forward-KL）目标，保留下游场景动态。

### 算法流程（文字描述）
1. 加载预训练双向视频扩散模型（如Wan2.2-5B）。
2. 可选：加载已有双向世界模型权重进行二次微调。
3. 第一阶段：用相机控制数据集微调模型，使模型理解相机运动参数。
4. 第二阶段：使用DMD + GAN + forward-KL损失，将微调后的模型蒸馏为少步（如1~4步）推理的交互式世界模型。
5. 推理时：用户输入当前帧+历史帧（可选）+动作/相机指令，模型直接生成下一帧，循环执行实现长程推演。

## 3. 实验设计
- **数据集/场景**：未详细说明具体数据集名称，但提到测试了“真实世界相机控制”场景，并与minWM对比。
- **Benchmark**：主要在可控性（相机控制是否失效）、长期展开稳定性、模式坍塌检测上评估。
- **对比方法**：主要对比minWM（开源因果框架）。结果：minWM在部分场景失去可控性，而BiWM保持真实相机控制。
- **无定量指标对比表格**，仅定性描述表现优于因果模型。

## 4. 资源与算力
- **显式说明**：在**8×H200 GPU**上训练，每阶段仅需**几百步即可收敛**。
- 模型规模覆盖1.3B（Wan2.1）到22B（LTX-2.3），未分开给出具体耗时。
- 提供了NVFP4 4位流水线，暗示可在更低资源下运行。
- 未提供精确训练时间（如多少小时）。

## 5. 实验数量与充分性
- **实验组数**：主要报告了1种对比（vs minWM）、4种模型规模、2种历史压缩风格、2种额外损失（GAN+forward-KL）的消融。整体实验数量偏少，缺乏大规模系统对比。
- **充分性评价**：
  - **局限**：缺乏量化指标（如FVD、FID、PSNR、LPIPS等）和开源基准对比；未在多个标准数据集（如UCF101、Something-Something）上评估；长程推演稳定性的定量测量未给出。消融实验偏弱。
  - **客观性**：声称BiWM在可控性上优于minWM，但未展示失效case的对比细节，存在自评价偏差风险。
- **结论**：实验从概念上证明了框架有效性，但充分性不足，需更多定量实验支撑。

## 6. 论文的主要结论与发现
- BiWM首次实现了开源双向自回归交互式世界模型，仅需两步训练即可从预训练骨干构建可控世界模型，收敛极快（数百步）。
- 支持多尺度模型和可插拔历史压缩、4位量化，资源友好。
- 提出的对抗前向KL目标有效缓解了DMD的模式退化，保留场景动态。
- 在相机控制任务上优于因果框架minWM，长期展开稳定。

## 7. 优点
- **框架创新**：首个全开源双向自回归视频世界模型框架，填补领域空白。
- **训练效率**：两步替代四步，且数百步收敛，极大降低资源门槛。
- **通用性**：单一配方覆盖多种主流预训练骨干（Wan、Hunyuan、LTX），且支持二次微调现有双向模型。
- **工程化设计**：集成历史压缩、4位量化、对抗性正则化，兼顾性能与实用性。
- **开源承诺**：降低研究门槛，促进可复现研究。

## 8. 不足与局限
- **实验不充分**：缺乏量化指标和标准基准对比，仅定性描述；消融实验不够全面。
- **评估偏差**：主要与minWM对比，未与更多双向自回归基线（如Yume-1.5）或因果蒸馏方法对比。
- **应用限制**：未测试多物体交互、复杂动作控制场景；历史压缩可能引入额外失真；4位量化对生成质量的损失未量化分析。
- **复现困难**：未提供具体数据集、超参、蒸馏步数等细节，代码开源但可能仍需适配。
- **理论分析不足**：为何DMD模式退化被GAN+forward-KL缓解缺乏深入理论解释。

（完）

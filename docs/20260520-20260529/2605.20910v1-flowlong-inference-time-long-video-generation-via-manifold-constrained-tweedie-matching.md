---
title: "FlowLong: Inference-time Long Video Generation via Manifold-constrained Tweedie Matching"
title_zh: FlowLong：基于流形约束Tweedie匹配的推理时长时间视频生成
authors: "Jangho Park, Geon Yeong Park, Gihyun Kwon, Jong Chul Ye"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20910v1"
tags: ["query:tfree-diff"]
score: 9.0
evidence: 基于流形约束特威迪匹配的无训练长视频生成方法
tldr: 长视频生成面临双向模型架构耦合、自回归模型漂移误差和重复运动的问题。本文提出FlowLong，一种架构无关的推理时方法，通过重叠滑动窗口与Tweedie匹配施加流形约束和时间一致性，并利用随机早期阶段采样同步噪声轨迹。实验表明，无需训练即可生成长度数倍于窗口的视频，时间一致性和视觉质量优于基线，且可扩展至音视频联合生成与文本到3DGS。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20910v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 697, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 481, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1436, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1426, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20910v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1446, \"height\": 314, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.20910v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20910v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 519, \"height\": 171, \"label\": \"Table\"}]"
motivation: 现有训练无关长视频生成方法中，双向模型架构耦合导致质量下降，自回归模型因曝光偏差积累漂移且产生重复运动模式。
method: 通过重叠滑动窗口生成相邻段，利用Tweedie匹配融合预测的干净样本以约束流形和时序一致性；在高噪声阶段注入新鲜噪声同步轨迹，再切换确定性ODE采样保持保真度。
result: 在多种视频扩散模型上生成数倍于原生窗长的视频，时间一致性和视觉质量优于训练无关和自回归基线，并能无微调扩展至音视频联合生成与文本到3DGS。
conclusion: FlowLong是一种简单、架构无关的推理时长视频生成方法，通过流形约束和随机同步有效克服漂移与重复问题，具有强泛化性。
---

## 摘要
将视频扩散模型的生成范围扩展到长序列仍然是一个长期且重要的挑战。现有的免训练方法分为两类：双向模型的扩展，这类方法紧密耦合到特定架构且面临长序列质量下降问题；以及自回归模型，由于暴露偏差累积漂移误差且倾向于产生重复运动模式。为解决这些问题，我们提出了一种新颖但简单的推理时方法用于长视频生成，该方法与架构无关且无需额外训练。我们的方法通过重叠滑动窗口生成长视频，其中来自相邻窗口的预测干净样本通过\emph{Tweedie匹配}进行混合，以在重叠区域同时施加\textbf{流形约束和时间一致性}。随后，\emph{随机早期阶段采样}通过在高噪声阶段每次Tweedie匹配校正后注入新鲜噪声来同步每个窗口的轨迹，然后过渡到确定性ODE采样以保留精细的视觉保真度。应用于各种视频生成模型，我们的方法生成比原生窗口长度长数倍的视频，同时在时间一致性和视觉质量上优于免训练和自回归基线，并可进一步扩展到音频-视频联合生成和文本到3DGS，无需任何微调。

## Abstract
Extending the generation horizon of video diffusion models to long sequences remains a long-standing and important challenge. Existing training-free approaches fall into two categories: extensions of bidirectional models, which are tightly coupled to specific architectures and suffer from quality degradation over long horizons, and autoregressive models, which accumulate drift errors due to exposure bias and tend to produce repetitive motion patterns. To address these issues, we propose a novel but simple inference-time approach for long video generation that is architecture-agnostic and requires no additional training. Our method generates long videos via overlapping sliding windows, where predicted clean samples from adjacent windows are blended via \emph{Tweedie matching} to enforce both \textbf{manifold constraint and temporal consistency} across overlap regions. \emph{Stochastic early-phase sampling} then synchronizes per-window trajectories by injecting fresh noise after each Tweedie matching correction in the high-noise phase, before transitioning to deterministic ODE sampling to preserve fine-grained visual fidelity. Applied to various video generation models, our method generates videos several times longer than the native window length while outperforming both training-free and autoregressive baselines in temporal consistency and visual quality, and further extends to audio-video joint generation and text-to-3DGS without any fine-tuning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：视频扩散模型通常受限于训练时所用的固定帧窗口（native window），无法直接生成长于该窗口的视频序列。直接外推会导致严重质量下降。
- **现有方法局限**：
  - **双向模型扩展**（如FIFO-Diffusion、RIFLEx、UltraViCo）：紧密耦合特定架构，长序列时质量退化，视觉伪影累积。
  - **自回归模型**（如CausVid、Self-Forcing等）：依赖KV-cache复用，暴露偏差导致误差漂移和运动重复，且需要从双向教师模型蒸馏，难以快速适配新架构。
- **论文目标**：提出一种**训练无关、架构无关**的推理时方法，能够将预训练的流模型（flow-based diffusion model）扩展到任意长视频生成，同时保持时间一致性和视觉质量，并可推广到音视频联合生成和文本到3DGS任务。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将长视频生成视为**多块采样轨迹的协调问题**：从独立噪声初始化多个重叠的视频块（chunks），通过约束相邻块在重叠区域的帧一致来生成连贯长视频。
- 关键在于**在采样过程中施加流形约束**，并**打破各块ODE轨迹的惯性**，使其相互对齐。

### 关键技术细节
1. **Tweedie匹配（Tweedie Matching）**
   - 将长视频划分为K个重叠的F帧块，相邻块共享O帧重叠区域。
   - 在每一步去噪中，利用Tweedie公式得到每个块的干净估计 $\hat{x}_0^{(k)}$，然后对重叠帧进行**凸组合插值**：
     \[
     \bar{x}_0^{(k)}[j] = (1-\lambda_j)\hat{x}_0^{(k)}[j] + \lambda_j \hat{x}_0^{(k+1)}[j']
     \]
     其中$\lambda_j$线性地从0到1递增，保证边界处完全一致。
   - 该更新等价于在流形上的投影，源自扩散逆问题求解器（DDS）中的梯度引导。

2. **随机早期阶段采样（Stochastic Early-Phase Sampling）**
   - 在去噪的早期（高噪声阶段，$t \ge t^*$），将确定性ODE更新改为随机SDE更新，注入新鲜噪声：
     \[
     x_s = (1-s)\bar{x}_0 + s \, (\sqrt{1-\eta_t}\,\bar{x}_1 + \sqrt{\eta_t}\,\epsilon)
     \]
   - 目的是打破各块初始独立噪声导致的轨迹惯性，促使跨块混合。
   - 后期（$t < t^*$）恢复为确定性ODE，保持精细视觉保真度。

3. **多块聚合**：所有成对更新可归约为一次在全局缓冲区上的加权平均，每个全局帧仅写入一次，消除重复计算。

4. **跨模态扩展**：对音视频联合生成，以帧率比对齐两种模态的块几何，分别用Tweedie匹配和随机采样，相互独立但时间同步。对文本到3DGS，在视频潜在空间外推后解码并输入前馈3D重建器。

## 3. 实验设计

- **任务**：
  - 文本到视频长视频生成（30秒和60秒）
  - 音频-视频联合生成
  - 文本到3DGS场景生成
- **数据集与prompt**：
  - 100个MovieGen Bench文本prompt（视频）
  - 100个SceneBench文本prompt（3DGS）
- **骨干模型**：
  - 视频：Wan 2.1-T2V-1.3B、LTX-2（14B）
  - 3DGS：Wan 2.1-T2V-14B + AnySplat
- **对比基线**：
  - 双向模型：RIFLEx、UltraViCo
  - 自回归模型：CausVid、Self-Forcing、Deep-Forcing、∞-RoPE、LongLive
  - 3DGS：VIST3A（自带AnySplat）
- **评估指标**：VBench的7个维度（美学质量、成像质量、背景一致性、主体一致性、运动平滑度、动态程度、时序闪烁），并报告总体分。

## 4. 资源与算力

- **硬件**：单块NVIDIA H100 GPU。
- **训练时长**：论文明确声明**无需任何训练**，所有方法均为推理时执行，因此没有训练时长。
- **额外说明**：未见具体推理时间或显存消耗数据，但提到方法高效，可在单卡运行。

## 5. 实验数量与充分性

- **实验数量**：
  - 长视频生成：两个时长设定（30s、60s），每设定100个prompt，包含7个指标。
  - 文本到3DGS：一个场景集（100 prompts），统计高斯数量、高置信度数量、置信度logit。
  - 消融实验：比较Tweedie匹配与其它重叠融合方式（如直接在含噪空间混合），以及全SDE、全ODE与混合采样策略。
- **充分性分析**：
  - 对比了当前最主流的双向和自回归基线，覆盖了训练无关和基于蒸馏的方法。
  - 定量和定性结果均提供，定性有视频示例（补充材料）。
  - 消融实验直接验证了两个核心组件的贡献。
  - 跨任务扩展（音视频、3DGS）验证了方法的通用性。
  - 总体上实验较为充分、客观，指标选择合理（VBench为公认视频生成评估基准）。但缺少与更多近期方法的对比（如FramePack、PFP等），且未提供用户研究或长期序列（>60s）的验证。

## 6. 主要结论与发现

- FlowLong在30秒和60秒视频生成中，总体VBench得分均超过所有基线，尤其在**动态程度（Dynamic Degree）** 指标上大幅领先，表明其生成的视频运动多样性好，不会重复。
- 相比自回归模型，FlowLong消除了曝光偏差和漂移误差；相比双向扩展方法，不依赖架构修改且质量更高。
- 在文本到3DGS任务中，生成的高斯数量比VIST3A多1.64倍，高置信度高斯数量多约3.3倍，置信度logit显著提升，证明外推后的视频潜在空间能产生更稠密、更可靠的3D场景。
- 消融实验证实：
  - Tweedie匹配（在干净估计空间混合）优于直接在含噪潜在空间混合。
  - 随机早期阶段采样（混合SDE/ODE）优于纯ODE（一致性差）和纯SDE（质量下降）。

## 7. 优点

- **训练无关、架构无关**：直接修改采样过程，无需微调或改变模型结构，可即插即用到任意流式视频扩散模型。
- **消除暴露偏差**：各块从独立噪声并行采样，不用KV-cache，从根本上避免误差累积。
- **运动多样性好**：独立初始化各块，打破自回归模型的重复运动模式。
- **跨模态泛化强**：同一框架无缝扩展到音频-视频和3DGS，无需任何任务特定调整。
- **实现简单**：Tweedie匹配本质上仅为重叠区域帧加权平均，随机采样部分控制参数少，易于实现。
- **理论优雅**：将长视频生成建模为逆问题，用流形约束的梯度引导实现，数学形式闭环。

## 8. 不足与局限

- **局部一致性限制**：重叠约束仅保证相邻块间的局部一致性，对于极长视频（如数分钟）可能无法维持全局语义连贯性（如场景变换、叙事结构）。论文自身承认这一局限性。
- **实验覆盖范围有限**：最长只到60秒，未测试更极端的长度（如数分钟或无限视频）。对比的基线未包含最近的一些方法（如FramePack、PFP、Self-Forcing++），可能低估了自回归方法的最新进展。
- **缺乏运行效率分析**：未报告推理时间、显存占用等计算成本，对于实际应用部署的可行性不明确。
- **缺少用户研究**：仅用VBench自动指标，未进行人类偏好评估，可能无法完全反映感知质量。
- **音频-视频任务仅定性展示**：无定量指标（如音频同步质量），说服力稍弱。
- **超参数依赖**：重叠帧数O、随机阈值t*需要手动设定，论文未提供自适应选择的规则，可能影响通用性。

（完）
